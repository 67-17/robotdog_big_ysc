# 启动避障集成修改记录

> 规则：从 2026-08-06 起，本次“起步至第一个巡航/巡检点”避障集成涉及的每一批文件修改，都必须在本文档留下可追溯记录。记录包括改动文件、改动内容、原因、风险与验证结果。硬件实测结果只能在真实上机后填写，不能用静态测试代替。

## 记录 0：导入避障核心模块（补录）

- 时间：2026-08-06 15:16（依据文件时间补录）
- 改动文件：
  - `mission_lite3/startup_avoidance/__init__.py`
  - `mission_lite3/startup_avoidance/controller.py`
  - `mission_lite3/startup_avoidance/model.py`
  - `mission_lite3/startup_avoidance/tracking.py`
  - `mission_lite3/startup_avoidance/vision.py`
- 具体内容：从 `adoidance_ws.zip` 中提取纯算法模块，建立主任务内部包 `startup_avoidance`。没有导入独立 ROS `/cmd_vel` 发布器，也没有启动压缩包中的 `run_avoidance.py`。
- 原因：必须让避障与主任务共用同一个 `Lite3MotionController`，防止两个速度发布源同时控制机器人。
- 当前状态：4 个算法文件仍是压缩包原始副本，内部导入路径尚未改为相对导入，运行桥接、主任务入口、配置和测试尚未完成。
- 风险：此时包无法可靠导入，不应上机运行。
- 验证：仅确认文件存在；未进行硬件操作。

## 记录 1：建立强制修改日志

- 时间：2026-08-06（当前会话）
- 改动文件：`避障集成修改记录.md`
- 具体内容：新增本文件，并规定以后每一批修改必须同步记录。
- 原因：满足项目负责人对变更可追溯性的要求，便于后续审查、回滚和上机排障。
- 风险：无运行时影响。
- 验证：文件创建成功。

## 记录 2：修正算法包导入并补充控制器硬保护

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `mission_lite3/startup_avoidance/controller.py`
  - `mission_lite3/startup_avoidance/tracking.py`
  - `mission_lite3/startup_avoidance/vision.py`
- 具体内容：
  - 将原独立工程的 `avoidance.*` 绝对导入改成 `startup_avoidance` 包内相对导入，避免依赖外部同名包。
  - 新增 `distance.emergency_stop_m` 距离下限；超声距离到达或低于该值时锁存 `FAULT`，输出全零速度。
  - 目标连续确认期间将前进速度从 `0.08 m/s` 改成 `0`，避免确认的 5 帧中继续接近障碍物。
  - 将写死的“避障 2 次后结束”改为读取 `decision.required_avoidances`。
- 原因：保证模块可作为主任务子包导入，并把近距离碰撞风险和任务结束条件显式纳入安全配置。
- 风险：配置尚未加入这两个新字段；在记录 3 完成前直接运行控制器会触发缺少键错误。
- 验证：待配置与单元测试完成后统一验证；未进行硬件操作。

## 记录 3：新增启动避障配置与启动前严格校验

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
- 具体内容：
  - 新增 `startup_avoidance` 配置，保留压缩包的相机、HSV、区域、跟踪、速度、新鲜度、回线和 2 次避障参数。
  - 新增 `0.20 m` 紧急停车距离与 `startup_avoidance_runs` 运行日志目录。
  - 严格校验布尔值、正整数、HSV 三元组、奇数形态学核、图像区域顺序、安全距离顺序、跟踪阈值、速度上限、回线参数与日志目录。
  - 严格加载模式要求 `robot.yaml` 必须显式提供该配置段。
- 原因：避免错误参数在机器人已经运动后才被发现，并保证避障速度不突破主运动控制器上限。
- 风险：默认启用；真实运行前必须确认 `/dev/video0` 确实是用于识别红色锥桶的前向相机。
- 验证：配置文件仍需通过加载测试和新增边界测试；未进行硬件操作。

## 记录 4：新增主任务内部避障运行桥接层

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `mission_lite3/startup_avoidance/runtime.py`
  - `mission_lite3/startup_avoidance/__init__.py`
- 具体内容：
  - 新增后台“只保留最新帧”的相机读取器，防止推理速度低于相机帧率时处理历史积压画面。
  - 复用主任务的 `StateReader` 和 `Lite3MotionController`，不创建第二个 ROS 节点或第二个 `/cmd_vel` 发布器。
  - 仅在超声传感器时间戳变化时把新样本加入中值窗口，避免同一个样本在高速循环中被重复计权。
  - 按“检测、跟踪、控制、同步活动目标、发送速度”顺序执行，并在故障或结束时写 JSONL 证据。
  - 任意异常均向上抛出；无论成功或失败都在 `finally` 中停车、释放避障相机并关闭日志。
  - dry-run 直接返回模拟完成，不访问相机、ROS 或真实运动硬件。
- 原因：把压缩包算法安全地嵌入现有单进程任务，消除双速度发布器、陈旧图像和重复超声样本风险。
- 风险：桥接层尚未接入 `mission.py`，也尚未经过单元测试；此时主任务仍走旧的固定步进避障路线。
- 验证：待记录 5 和测试完成后统一验证；未进行硬件操作。

## 记录 5：接入主任务 PASS_OBSTACLE 状态

- 时间：2026-08-06（当前会话）
- 改动文件：`mission_lite3/mission.py`
- 具体内容：
  - 在 `PASS_OBSTACLE` 状态优先运行 `StartupAvoidanceRunner`。
  - 仅当配置启用且没有传入 `ignore_obstacles`、`ignore_ultrasound_obstacle` 时启用集成避障。
  - 避障成功后直接结束该状态，状态机下一步仍进入 `INSPECT_LEFT_OBJECT`，继续执行原有 `inspect_stop_1_arrive` 到第一个巡检点。
  - 避障结果失败或运行器抛出异常时任务中止，不回退到旧的固定横移避障。
  - 构造函数新增可注入的运行器工厂，仅用于隔离单元测试，不改变命令行调用方式。
- 原因：实现“从起步穿过两个障碍、回原线、再沿原路线到第一个巡检点”的单进程交接。
- 风险：显式关闭 `startup_avoidance.enabled` 或使用忽略障碍参数时，仍保留原项目旧路线，便于调试和回退；正常启用状态不会静默回退。
- 验证：待新增状态机测试确认调用、失败中止和后续首巡检路线保持不变；未进行硬件操作。

## 记录 6：新增避障控制、运行桥接和状态机交接测试

- 时间：2026-08-06（当前会话）
- 改动文件：`tests/test_startup_avoidance.py`
- 具体内容：新增 10 项聚焦测试，覆盖紧急距离故障锁存、确认期零平移、两次避障及每次回线、成功/故障清理、JSONL 日志、dry-run 零硬件访问、超声时间戳去重、主任务调用与失败不回退、首巡检路线保持、无效配置拒绝。
- 原因：这次修改涉及运动安全和状态机交接，必须用独立测试证明关键行为，而不能只确认代码可导入。
- 风险：测试使用模拟相机、模拟里程计和模拟运动对象，只证明软件控制流，不证明真实锥桶识别率、相机设备号、超声数据质量或机器人实际运动方向。
- 验证环境：计划使用 `C:\Users\67\AppData\Local\Programs\Python\Python312\python.exe -B`；系统默认 Python 3.9 缺少 OpenCV，且现有 `__pycache__` 不可写。
- 验证：待执行。

## 记录 7：修正测试临时目录的沙箱权限问题

- 时间：2026-08-06（当前会话）
- 改动文件：`tests/test_startup_avoidance.py`
- 具体内容：将两项 JSONL 运行器测试的临时目录从 Python 默认的 `C:\Users\67\AppData\Local\Temp` 改为工作区允许写入的 `I:\tmp`。
- 原因：首次执行结果为 10 项中 8 项通过、2 项错误；错误均发生在创建测试日志文件时，报错为 `PermissionError`，尚未进入被测避障逻辑。
- 风险：只改变测试输出位置，不改变机器人运行代码和日志默认位置。
- 验证：待重新执行新增测试。

## 记录 8：将运行器日志打开动作改为可注入并移除测试临时目录

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `mission_lite3/startup_avoidance/runtime.py`
  - `tests/test_startup_avoidance.py`
- 具体内容：运行器新增可选 `log_opener` 注入点；生产默认行为仍是追加写入 UTF-8 JSONL。测试使用 `StringIO` 检查成功决策和错误事件，不再创建随机临时目录。
- 原因：将测试目录改到 `I:\tmp` 后，测试仍挂起；线程栈证明阻塞发生在 `tempfile.mkdtemp()`，并未进入避障运行器。
- 风险：新增参数仅供测试注入，主任务未传入该参数，生产日志路径和格式不变。
- 验证：待重新执行新增测试。

## 记录 9：让内存测试日志可验证关闭动作

- 时间：2026-08-06（当前会话）
- 改动文件：`tests/test_startup_avoidance.py`
- 具体内容：新增 `InspectableLog`，记录运行器是否调用 `close()`，同时保留内存内容用于断言成功决策和错误事件。
- 原因：标准 `StringIO` 关闭后无法调用 `getvalue()`；运行器关闭日志是必须验证的正确行为，测试替身不能因此丢失证据。
- 风险：仅影响测试替身。
- 验证：待重新执行新增测试。

## 记录 10：更新三份项目文档中的起步避障说明

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `README.md`
  - `mission_lite3/README.md`
  - `项目说明.md`
- 具体内容：将已失效的“固定 8 步前进和固定横移”正式流程说明替换为视觉、超声、里程计融合的 2 次锥桶避障；记录确认停车、固定向左绕行、每次回线、第二次回线交接、首巡检路线、速度、安全阈值、故障中止、JSONL 日志和旧路线启用条件。
- 原因：源码行为已经改变，旧文档会让后续负责人按错误路线和参数排障。
- 风险：文档中的数值来自当前配置，不代表已经通过真实场地标定；以后修改配置时必须同步更新文档和本修改记录。
- 验证：待检查文档关键字与最终测试结果。

## 记录 11：恢复避障期间的主状态新鲜度保护

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `mission_lite3/startup_avoidance/runtime.py`
  - `tests/test_startup_avoidance.py`
- 具体内容：调用主 `StateReader.safety_error` 时改为 `require_fresh=True`，并新增测试确认同时要求超声和主状态新鲜。
- 原因：避障控制器自身检查图像、超声和里程计年龄，但不检查 IMU 更新时间；如果仅检查数值，停止更新的旧横滚/俯仰数据可能被误认为安全。
- 风险：真实运行中 IMU、里程计或超声超过主安全配置的 `state_max_age_s` 会更早中止，这是预期的故障安全行为。
- 验证：待重新运行专项测试。

## 记录 12：收紧会导致避障失效的配置边界

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `mission_lite3/config_loader.py`
  - `tests/test_startup_avoidance.py`
- 具体内容：
  - OpenCV HSV 色相限制为 `0..179`，并要求每个通道下界不高于上界。
  - 要求 `front_trigger_m` 严格大于 `emergency_stop_m`，避免到达触发点时总被紧急停车抢先中止。
  - 要求 `required_avoidances` 必须恰好为 2，防止误配置后只绕过一个锥桶就交给巡检路线。
  - 回线容差最大 `0.10 m`，且回线超时不得大于总避障超时。
  - 为以上危险配置增加拒绝测试。
- 原因：这些参数即使类型正确，组合错误仍会让检测、避障或交接行为失效，必须做跨字段校验。
- 风险：旧的自定义配置若使用超出上述安全边界的值，将在启动时被拒绝，需要人工确认后修正。
- 验证：待重新运行专项和配置测试。

## 记录 13：写入最终静态与本机测试证据

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `项目说明.md`
  - `避障集成修改记录.md`
- 输入证据：`adoidance_ws.zip` SHA256 为 `14E0D3DBEAF94CB8A780C63E1C08E2B0DAE70F2AF6538FB6D7B587311D0952CF`。
- 静态检查：8 个本次相关 Python 文件 AST 解析通过；配置严格加载通过；避障子包未发现 `create_publisher`、`rclpy` 或独立 `/cmd_vel` 发布代码。
- 专项测试：`tests/test_startup_avoidance.py` 共 11 项，11 项全部通过。
- 主任务烟雾测试：89 项中 88 项通过；唯一失败为 Windows 找不到 Unix `true` 命令。
- 全量回归：374 项中 373 项通过；唯一失败仍为 `test_audio_reporter_tts_fallback_clears_wav_error`，错误为 `true not found`，与避障代码无关。
- 环境说明：使用 Python 3.12.6、OpenCV 4.8.1、NumPy 1.26.2，并使用 `-B` 避免现有不可写 `__pycache__` 影响测试。
- 未验证边界：没有连接 `/dev/video0`、ROS2、前向超声、`/leg_odom2` 或真实机器人；左右方向、HSV 识别效果、2 次实物绕行、回线精度和到达第一个巡检点仍必须现场低速验证。
- 仓库说明：该目录没有 Git 元数据，不能用 `git diff/status` 生成变更清单；本文件是本次修改的主要追溯依据。
- 风险：文档与测试只能证明当前软件结构和模拟控制流，不能宣称硬件任务已成功。

## 记录 14：结束条件由障碍数量改为障碍区里程

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `mission_lite3/startup_avoidance/controller.py`
  - `mission_lite3/startup_avoidance/runtime.py`
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `tests/test_startup_avoidance.py`
- 具体内容：
  - 删除“必须避开 2 个障碍物才能结束”的 `required_avoidances` 条件。
  - 新增 `decision.finish_forward_m=2.40`，按初始朝向投影 `/leg_odom2` 位移判断是否穿过障碍区。
  - 0 个障碍时持续安全巡航，到达障碍区出口后停车交接。
  - 遇到 1 个或多个障碍时逐个执行确认、向左绕行、通过和回原线；只有已经到达出口且不处于未完成的绕行/回线时才结束。
  - dry-run 模拟 0 个障碍通过；JSONL 新增 `forward_progress_m`。
  - 新增 0 个、1 个和多个障碍物的控制器测试，并把配置测试改为校验障碍区长度。
- 原因：现场障碍数量不固定，可能为 0、1 或多个；固定计数会让少于 2 个时超时，也会让多于 2 个时提前交接。
- 风险：`2.40 m` 继承自旧路线的 `8 x 0.30 m` 障碍区长度，仍需现场用里程计核对实际出口位置。
- 验证：待执行专项测试。

## 记录 15：修正里程结束条件测试替身

- 时间：2026-08-06（当前会话）
- 改动文件：`tests/test_startup_avoidance.py`
- 首次测试结果：13 项中 9 项通过，1 项失败、3 项错误。
- 原因：
  - 运行器假控制器缺少新增的 `forward_progress_m` 日志字段。
  - dry-run 断言仍期待旧的固定 2 次避障计数，正确结果应为 0。
  - 多障碍测试重复构造控制器，覆盖了自定义的 `5.0 m` 出口距离。
- 具体内容：为假控制器补充前向里程，分别修正真实假控制器与 dry-run 的计数断言，并删除重复构造。
- 风险：仅修改测试代码，不影响机器人运行逻辑。
- 验证：待重新执行专项测试。

## 记录 16：同步障碍数量不固定的项目文档

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `README.md`
  - `mission_lite3/README.md`
  - `项目说明.md`
- 具体内容：将“固定避开 2 个锥桶”统一改为“障碍区内遇到几个就逐个避开几个，0 个时直接巡航”；明确 `finish_forward_m=2.40 m` 是出口判据，当前绕行和回线完成后才允许交接。
- 原因：文档必须与新的里程结束条件一致，避免现场人员继续按固定障碍计数理解任务。
- 风险：`2.40 m` 仍是旧路线推导值，不是现场重新测量值。
- 验证：0 个、1 个和多个障碍物专项测试共 13 项，当前全部通过；待执行全量回归。

## 记录 17：障碍数量不固定版本的最终验证

- 时间：2026-08-06（当前会话）
- 改动文件：
  - `项目说明.md`
  - `避障集成修改记录.md`
- 专项测试：13 项全部通过，分别覆盖 0 个障碍直接过区、1 个障碍绕行回线后过区、多个障碍逐个绕行直到出口，以及原有安全、故障清理和状态机交接。
- 全量回归：376 项中 375 项通过；唯一失败仍为 Windows 缺少 Unix `true` 的原有音频测试 `test_audio_reporter_tts_fallback_clears_wav_error`，与避障修改无关。
- 静态检查：5 个本次直接相关 Python 文件 AST 解析通过；严格配置加载得到 `finish_forward_m=2.40`；运行代码与新增测试中不存在 `required_avoidances`；当前三份项目文档中不存在固定两个障碍的表述。
- 未验证边界：没有连接真实相机、ROS2、超声波、里程计或机器狗。现场必须核对 `2.40 m` 是否正好对应实际障碍区出口；否则只调整 `startup_avoidance.decision.finish_forward_m` 并追加新记录。
- 风险：里程计累计误差会直接影响出口判断；如果障碍区实际长度与 `2.40 m` 不符，可能提前或延后切换到第一个巡检点路线。

## 记录 18：通用移动防走歪与抓取放置阶段安全修复

- 时间：2026-08-06（当前会话）
- 用户现象：机器狗在普通前进、后退或横移时会走歪；同时要求修复仪表盘识别后、机械臂抓取至放置流程中会肉眼阻止运行的问题，并且不破坏现有避障和任务流程。
- 改动文件：
  - `mission_lite3/lite3_motion.py`
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `mission_lite3/box_approach.py`
  - `mission_lite3/placement_letter_navigation.py`
  - `mission_lite3/mission.py`
  - `mission_lite3/arm/runtime/arm_grasp.py`
  - `mission_lite3/arm/runtime/place_controller.py`
  - `mission_lite3/arm/runtime/arm_task.py`
  - `tests/test_mission_lite3_smoke.py`
  - `tests/test_arm_runtime_safety.py`
  - `tests/test_box_approach.py`
  - `tests/test_pregrasp_red_align.py`
  - `tests/test_placement_letter_navigation.py`
  - `避障集成修改记录.md`

### 18.1 普通移动走歪的根因与修复

- 原因：原 `_move_distance()` 只用 `hypot(dx, dy)` 判断是否走够距离，普通前进、后退和横移始终发送固定 `vx/vy`、`wz=0`。横向漂移会被错误计入已完成距离，IMU 航向变化和横向偏移也不会被纠正，因此会出现斜着走并提前结束。
- 修复：以每段动作开始时的 `/leg_odom2` 位姿和 IMU 航向为参考坐标系，把实际位移分解为“沿目标方向进度”和“垂直目标方向误差”。只有沿目标方向的投影达到目标才完成，横向漂移不再算作前进距离。
- 修复：普通前进、后退、向左横移和向右横移统一使用方向无关的投影公式；横向误差通过受限的垂直速度分量纠正，包角后的航向误差通过受限 `wz` 纠正。
- 新配置：
  - `translation_path_hold_enabled=true`
  - `translation_cross_track_kp_s=1.0`
  - `translation_max_cross_track_correction_mps=0.04`
  - `translation_cross_track_deadband_m=0.003`
  - `translation_max_cross_track_drift_m=0.15`
  - `translation_yaw_hold_kp_s=1.2`
  - `translation_max_wz_correction_rad_s=0.12`
  - `translation_yaw_deadband_deg=0.30`
  - `translation_max_yaw_drift_deg=5.0`
- 安全行为：横向偏离超过 `0.15 m` 或航向偏离超过 `5 deg` 时停止并报错，不继续盲走；原有 `_motion_guard()`、速度总限幅、反馈缺失拒绝、动作超时和 `finally` 最终停车全部保留。
- 配置校验：新增布尔、有限数值、死区与最大漂移关系、纠偏速度与运动上限关系校验，危险配置在启动时直接拒绝。

### 18.2 抓取后运输姿态失败不得再次张夹爪

- 原因：夹爪已经成功闭合后，如果进入 `CARGO_POSE` 失败，旧结果仍可能报告 `object_held=false`；任务层会把它当作未抓到并再次重试，存在重新张开夹爪、物体掉落或机械臂碰撞的风险。
- 修复：所有“闭合成功、运输姿态随后失败”分支返回 `object_held=true`；关闭夹爪命令本身失败的分支不冒充已持物。
- 修复：任务层看到 `ok=false` 且 `object_held=true` 时，立即保留 `context.carried_bar=true` 并抛出任务中止，禁止第二次抓取，交给人工恢复。

### 18.3 放置已释放后回撤失败不得继续算作持物

- 原因：旧放置控制器在夹爪打开后的肩关节或肘关节回撤失败时抛出裸异常，丢失“物体已经释放”的阶段事实；上层可能继续认为夹爪仍持物。
- 修复：`PlaceResult` 新增 `released`。夹爪打开前失败返回 `object_held=true/released=false`；夹爪确认打开后任何回撤失败返回 `object_held=false/released=true`，并附带失败阶段、原因和已经执行的计划。
- 修复：肩关节半程回撤失败后仍禁止继续移动肘关节；`ArmTask` 在失败但已经释放时同步内部持物状态。

### 18.4 机械臂急停不得虚假报告成功

- 原因：底层源码明确说明当前硬件没有已验证的机械臂急停命令，但旧 `abort()` 是空操作并返回成功，日志会误导现场人员认为机械臂已经安全停止。
- 修复：真机运行时不执行未经验证的回零或收臂动作，返回 `ok=false`、`stage=MANUAL_RECOVERY_REQUIRED` 和明确原因；dry-run 仍保留模拟成功。

### 18.5 抓取前距离窗口与放置最终距离

- 抓取前超声有效下限由 `0.28 m` 调整为 `0.25 m`，最终抓取只接受 `0.25 m <= distance < 0.30 m`。`0.27 m` 的轻微过冲可安全停车，`0.24 m` 视为过近并立即失败，不允许继续前进。
- 字母箱导航不再因前进预算耗尽就返回完成。只有目标字母居中且前超声达到停止距离才允许放置；预算耗尽但仍过远时返回 `forward_budget_exhausted_without_final_center`，防止机械臂伸向过远箱体。

### 18.6 当前定位方式与 AprilTag 决策

- 静态搜索确认当前项目没有 AprilTag、ArUco、fiducial、`solvePnP` 或标签位姿解算代码。
- 当前定位组合是：`/leg_odom2` 提供平面位移，IMU 提供航向，前后超声提供近距离安全与最终距离，摄像头负责障碍物、仪表盘、红色目标和字母箱视觉对齐；它不是视觉 SLAM，也不是标签定位。
- 如果现场允许新增标签，本项目选择 AprilTag 作为巡航点和工位的绝对位姿校正层，理由是远距离、倾斜视角和光照变化下通常比普通 ArUco 更稳健。
- 本次没有直接启用 AprilTag，因为尚无标签尺寸、标签 ID、相机外参和标签场地坐标；伪造这些参数会让现有无标签任务直接失效。AprilTag 将作为里程计和 IMU 的绝对校正，不替代运动中的闭环控制。

### 18.7 验证结果与未改变范围

- 专项回归：运动、机械臂、抓取距离、预抓取和字母箱导航共 238 项，237 项通过；唯一失败是既有 Windows 音频测试找不到 Unix `true` 命令。
- 机械臂分支纠正后复测：`tests/test_arm_runtime_safety.py` 共 17 项，17 项全部通过。
- 全量回归：388 项中 387 项通过；唯一失败仍是 `test_audio_reporter_tts_fallback_clears_wav_error`，错误为 `true not found`，与本次修改无关。
- 新增覆盖：横向漂移不能计作前进进度、前进横向/航向纠偏、倒退纠偏、横移纠偏、危险参数拒绝、运输姿态失败保留持物、禁止二次抓取、释放后回撤失败、虚假急停、`0.27 m` 接受、`0.24 m` 拒绝、字母箱过远禁止完成。
- 未改变：启动避障仍按障碍区里程结束，遇到几个障碍就逐个避开几个，0 个直接巡航；没有改变既有路线、巡检结果合同、相机新鲜度、超声新鲜度、姿态安全门槛或机械臂关节协议。
- 真机边界：本机测试没有连接机器狗、ROS2、相机、超声、IMU 或机械臂，因此不能把软件回归通过写成“真机保证直行”。现场必须先低速测量每段的横向误差和航向误差，再只调整上述纠偏增益、死区和限幅，并为每次调整继续追加记录。

## 记录 19：本地主要代码差异覆盖到机器狗工作空间

- 时间：2026-08-07 15:34（当前会话）
- 本地来源：`I:\lite3\robot_competition_full_code`
- 远端目标：`ysc@192.168.1.103:/home/ysc/my_sw/robot_competition`
- 操作原则：覆盖前先完整备份原代码和 Git 状态；只替换本地与远端内容不同的主要代码文件；不删除远端独有文件、运行日志或历史结果；不启动机器狗和任务程序。

### 19.1 远端原代码备份

- 备份目录：`/home/ysc/my_sw/.codex_backups/robot_competition_before_local_deploy_20260807_151256`
- 备份内容：部署范围内的原始代码、完整 `.git`、覆盖前 `git status`、覆盖前 `git diff`、部署清单和原代码归档 `original_code.tar`。
- 原代码归档校验：`original_code.tar` 共 `35,358,720` 字节、`666` 个归档条目，SHA256 为 `8dfaf876467cc82fa169775ac7168b582dca0f71d0621c417ad3556317b3f293`。
- 覆盖前远端仓库已经存在未提交修改，其中包括：
  - `mission_lite3/arm/runtime/strip_detector_grasp_config.json`
  - `mission_lite3/config/field.yaml`
  - `mission_lite3/config_loader.py`
- 处理方式：没有清理或回退这些原有修改；其覆盖前版本和 Git 差异已保存在上述备份中，以便出现问题时按原样恢复。

### 19.2 差异比较与覆盖范围

- 纳入比较：`mission_lite3/`、`scripts/`、`systemd/`、`tools/`、`tests/`，以及项目根目录的程序、脚本和文档。
- 明确保留：`26比赛资料/`、`lite3资料/`、运行日志目录、运行结果目录、远端独有文件和历史运行数据。
- 本地候选文件共 `155` 个；按 CRLF/LF 换行差异归一化后，`118` 个与远端内容一致，没有上传或改写；实际替换 `37` 个。
- 实际替换文件：
  - 根目录：`.gitattributes`、`.gitignore`、`README.md`、`项目说明.md`、`避障集成修改记录.md`
  - 主任务：`mission_lite3/box_approach.py`、`box_center_alignment.py`、`config_loader.py`、`lite3_motion.py`、`mission.py`、`placement_letter_navigation.py`、`route_validation.py`、`README.md`
  - 机械臂：`mission_lite3/arm/runtime/arm_grasp.py`、`arm_task.py`、`place_controller.py`、`strip_detector_grasp_config.json`
  - 配置：`mission_lite3/config/field.yaml`、`robot.yaml`
  - 启动避障：`mission_lite3/startup_avoidance/__init__.py`、`controller.py`、`model.py`、`runtime.py`、`tracking.py`、`vision.py`
  - 启动脚本：`scripts/run_full_mission.sh`、`run_pickup_transfer_mission.sh`
  - 测试：`tests/test_arm_runtime_safety.py`、`test_box_approach.py`、`test_box_center_alignment.py`、`test_box_center_mission.py`、`test_mission_lite3_smoke.py`、`test_pickup_transfer.py`、`test_placement_letter_navigation.py`、`test_pregrasp_red_align.py`、`test_startup_avoidance.py`、`tests/data/placement_letter_d.jpg`
- 差异载荷：共 `4,603,392` 字节，SHA256 为 `fc97595e2d5d16898483c6ae3af7a9301751ebf1426750efc828004a10c368ed`。
- 替换方法：先上传到独立临时目录并逐文件校验，再使用目标文件同目录临时文件和原子 `mv` 逐个替换；已有文件权限保持不变，新 Shell 脚本按 `755` 处理。没有执行整目录删除或整体覆盖。

### 19.3 覆盖后验证

- 清单验证：归一化内容清单 `155/155` 一致，结果 `NORMALIZED_MANIFEST_OK=155`。
- 原始哈希验证：实际部署的 `37/37` 个文件与本地原始 SHA256 一致，结果 `DEPLOYED_RAW_HASH_OK=37`。
- 远端环境：Python `3.8.10`。
- 远端全量回归：执行 `python3 -B -m unittest discover -s tests`，共运行 `388` 项，耗时 `95.597s`，结果 `OK`。
- 说明：本次远端 Linux 环境下原来在 Windows 因缺少 Unix `true` 命令而失败的音频测试也已通过，因此远端结果为 388 项全部通过。

### 19.4 风险、回退与未验证边界

- 本次软件覆盖和回归测试没有启动任务进程，也没有向机器人发送运动、机械臂或抓放命令。
- 哈希一致与单元测试通过只证明文件部署正确、当前测试覆盖内的软件逻辑可运行，不能证明相机设备号、ROS2 话题、超声、IMU、里程计、机械臂通信、实际避障方向、直行纠偏参数或抓放动作已经通过真机验证。
- 首次真机运行前仍需检查设备与话题，再进行低速、可随时人工断电的分阶段测试；不得直接按完整比赛任务宣称硬件可用。
- 如本次覆盖导致异常，使用 `19.1` 的备份目录恢复；恢复时也必须先记录当时现场状态，并在本文件追加新的回退记录，不能静默覆盖。

## 记录 20：BOOT_CHECK 扬声器预热、完整服务日志与持续相机读取

- 时间：2026-08-07 19:25（当前会话）
- 用户限定范围：只修复 BOOT_CHECK 扬声器预热、音频服务响应日志、前置 RTSP 持续读取、可终止采集进程、自动重连状态与时间线日志；不修改路线、运动参数、识别算法、抓取动作、放置动作或机械臂通信协议。
- 改动文件：
  - `mission_lite3/persistent_camera.py`（新增）
  - `mission_lite3/camera.py`
  - `mission_lite3/mission.py`
  - `mission_lite3/audio.py`
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `tools/remote_audio_server.py`
  - `scripts/run_full_mission.sh`
  - `tests/test_mission_lite3_smoke.py`
  - `tests/test_box_center_mission.py`
  - `避障集成修改记录.md`

### 20.1 BOOT_CHECK 扬声器预热和完整音频响应日志

- 原因：第一次播报前 ALSA/扬声器链路可能仍处于冷启动状态；旧任务也没有把每次 UDP 服务响应的完整 JSON、请求编号和时间写进统一任务日志，现场无法区分“请求未发出、服务未响应、响应错误、播放失败”。
- 修复：`LargeQuadrupedMission._state_boot_check()` 在 BOOT_CHECK 调用 `AudioReporter.prewarm()`。默认发送 `warmup` UDP 请求，让远端服务播放 `0.8 s` 的 16 kHz 单声道静音 WAV，提前初始化 ALSA，不改变正式播报内容。
- 配置：新增并校验 `audio.prewarm_enabled=true` 和 `audio.prewarm_duration_s=0.8`，时长只允许 `0.1~3.0 s` 的有限数值。
- 日志：每次音频服务请求、完整响应和无响应错误均记录本地时区毫秒时间、`command`、`request_id`、重试序号、响应地址及完整 JSON。固定前缀为 `[audio-service] request/response/no_response`，兼容原有日志检索。
- 完整任务日志：`run_full_mission.sh` 在 `logs/full_mission/mission_YYYYmmdd_HHMMSS.log` 先写启动行，再用 `tee -a` 追加全部 Python 标准输出和错误输出；分别保存任务退出码与 `tee` 退出码。任务本身成功但日志写盘失败时改为退出码 `4`，不得虚假报告“完整日志已保存”。
- 兼容风险：机器狗上的新客户端与 `192.168.1.120` 音频服务必须同时更新；旧服务不认识 `warmup` 时 BOOT_CHECK 只会记录 `prewarm_warning` 并继续，不能据此宣称首次播报问题已经在真机解决。

### 20.2 前置 RTSP 持续最新帧读取

- 原因：在抓取、转向和放置阶段反复 `release/open` RTSP 会重复建立 FFmpeg/OpenCV 会话，现场可能出现数秒阻塞、句柄未释放、缓存旧画面或主任务卡死；把 `VideoCapture.read()` 放在普通线程中也无法可靠终止底层阻塞调用。
- 修复：新增一个从 BOOT_CHECK 持续到任务统一清理阶段的 `PersistentLatestFrameReader`。OpenCV `VideoCapture` 位于独立 `spawn` 进程内，后台持续读取，只发布最新帧；主任务等待新序号帧，不再直接进入可能长期阻塞的 RTSP 读取。
- 共享：`front_camera` 为读取器所有者，`wide_camera` 共享同一个读取器但保留独立消费序号和数字变焦视图。共享视图的 `release()` 是无操作，只有任务最终清理时所有者才关闭 RTSP。
- 可终止：监督线程检测采集进程退出、打开超时和画面超时。正常停止等待 `0.25 s`，仍阻塞时依次 `terminate` 和 `kill`，随后重建新一代进程。
- 并发保护：帧交换使用两个共享缓冲槽和奇偶发布版本，不使用可能在强制终止后永久锁死的进程共享锁。消费者只有在复制前后槽号、序号、时间和偶数版本完全一致时才接受帧。
- 旧数据保护：每次启动新一代采集进程时先把有效帧时间清零；新帧发布前不返回旧缓冲。迟到的旧代 `connected/worker_stopped` 消息只记录为 `stale_worker_status`，不得覆盖当前连接状态或清零当前失败计数。
- 可观测性：日志包含时间、连接事件、采集代数、重连次数、连续失败数、累计失败数和事件细节；放置导航 JSONL 同步写入相机状态快照。

### 20.3 验证、哈希与未验证边界

- AST：本次 8 个直接相关 Python 文件解析通过，结果 `AST_OK 8`。
- 配置：严格加载通过，结果 `CONFIG_OK True 0.8 rtsp://192.168.1.120:8554/test`。
- 专项：BOOT_CHECK 预热、完整响应、超时、共享读取器、阻塞进程终止、重连旧缓存拒绝、双缓冲竞争拒绝和旧代状态隔离测试全部通过。
- 全量回归：`py -3.12 -B -m unittest discover -s tests` 共 `396` 项，`396/396` 通过，耗时 `32.590 s`。原 Windows 环境缺少 Unix `true` 的测试已改为当前 Python 解释器空命令，不改变运行代码。
- Shell 文件：shebang、LF 换行和脚本结构检查通过；本机 `bash -n scripts/run_full_mission.sh` 因 WSL 创建实例被系统拒绝（`Bash/Service/CreateInstance/E_ACCESSDENIED`）而未完成，因此不能把本机结果写成 Linux Shell 语法已执行通过。
- SHA256：
  - `persistent_camera.py`：`9445E1520EA1C890B4734483312150DCE57D90656DD5A0AF041C48661A80CD89`
  - `camera.py`：`1C36A31C8F22D50CC8DCC5CA5DCEFDCFCEBAE4C8A26E25AC65A80CC08944C0A5`
  - `mission.py`：`C2BBC9F24F16B6DAAE07D7726314EACBB0FEE8BBF041A45045609AFFD4F1FF25`
  - `audio.py`：`60D809D1ADAA07445AD24E77C3078EDED0A64C17D06ED4135A11985D7502822B`
  - `config_loader.py`：`BA0BD2A0D56B9E832AB8835ECCCC9ABCFA2C7F6EC1B1068F7DA01A9214556D09`
  - `robot.yaml`：`11A1BC15CBCD34DA439D4464A5358FBDA6E647F89145BEFFEB5ACB3B221BFB83`
  - `remote_audio_server.py`：`DCD2A492D44F09E2DAFF7EC632F2FB1F750A2DFD3987214BD922143E8B0FDB6F`
  - `run_full_mission.sh`：`E0298015430C16D74C5F8C41DB0DF2A308B0F09C5F0DD26FA4753557F536571B`
  - `test_mission_lite3_smoke.py`：`7732543628273CE17AEC8ED2B65E6E634C16CCCC830AC714394F77671C43FA79`
  - `test_box_center_mission.py`：`DD37CCE16A2D65D6B65C4272693FD2655F1A0746D7F8DE876184E24E57E893F4`
- 清理：已删除系统临时目录内 6 个已应用的补丁载荷文件；没有删除项目文件、远端文件或历史备份。
- 未部署：本记录对应代码当前只在 `I:\lite3\robot_competition_full_code`。没有覆盖 `192.168.1.103`，也没有更新或重启 `192.168.1.120` 音频服务，没有启动任务、相机、ROS2、运动控制或机械臂。
- 真机边界：单元测试不能证明 RTSP 可连接、采集进程能在目标 Linux/OpenCV 版本下终止、扬声器已预热、首次播报有声或完整任务不会卡死。部署后必须先静态检查和单项相机/音频验证，再低速分阶段运行。

## 记录 21：记录 20 修复部署到机器狗并建立强制备份部署规则

- 时间：2026-08-07 19:33（当前会话）
- 部署目标：`ysc@192.168.1.103:/home/ysc/my_sw/robot_competition`。
- 永久执行规则：从本记录开始，每一批本地项目修改都必须在同一批工作内完成“确认无任务运行 -> 备份远端覆盖前原件 -> 只上传本批差异 -> 远端逐文件哈希 -> 远端静态/单元测试 -> 更新本文档 -> 再备份并部署本文档”。没有备份或哈希不一致时禁止覆盖；部署与测试期间禁止自动启动机器狗任务。

### 21.1 覆盖前状态与备份

- 连接身份：主机名 `lite`，用户 `ysc`，Python `3.8.10`。
- 覆盖前未发现 `mission_lite3.run_mission` 或 `run_full_mission.sh` 任务进程。
- 工作空间原本已有上次部署留下的未提交修改和运行结果目录；本次没有执行 `git reset`、`git checkout`、整目录删除或清理这些现场内容。
- 主备份目录：`/home/ysc/my_sw/.codex_backups/robot_competition_before_camera_audio_20260807_193311`。
- 备份范围：本批 11 个目标路径中，覆盖前存在的 10 个文件逐目录保存；新增的 `mission_lite3/persistent_camera.py` 记录在 `missing_before_deploy.txt`，表示回退时应删除该新增文件，而不是伪造旧版本。
- 附加证据：`before_git_status.txt`、`before_git_diff.patch`、`original_sha256.txt`、`missing_before_deploy.txt` 和 `original_files.tar`。
- 覆盖前归档：`original_files.tar` SHA256 为 `dbafcdb35df3b2d4515d38a0559733a47a8e6b7fcc1c32c9af025c4d0fa5afa7`。
- 在部署记录 21 前又单独备份机器狗上的记录 20 文件到 `before_record21/避障集成修改记录.md`，其 SHA256 为 `f233dfda7b8244b7b83e72ffa8bd7e9554ac0e5928da240bc2dd1bdec7a2a1a3`。

### 21.2 部署范围与方法

- 部署包：本地与远端临时包 `lite3_camera_audio_deploy_20260807_193311.tar` SHA256 均为 `cb74dd7fa281d2f6c2db022bc3adf1062354f6100e1b6142964dd4db6f19cf38`。
- 实际替换 11 个文件：记录 20 列出的 10 个代码/配置/测试文件及 `避障集成修改记录.md`；没有覆盖其他源码、日志、识别结果或运行目录。
- 替换前先在远端暂存区逐文件核对记录 20 的预期 SHA256；全部匹配后才使用同目录临时文件逐个原子替换，并保留已有文件权限。`scripts/run_full_mission.sh` 最终权限固定为 `0755`。
- 首次部署结果：`DEPLOY_REPLACED=11`；部署后 11 个文件 SHA256 与本地全部一致。
- 本次按用户确认只使用同一个部署 IP `192.168.1.103`，没有再把 `192.168.1.120` 当作独立 SSH 部署主机。

### 21.3 远端验证与音频服务边界

- Shell：机器狗执行 `bash -n scripts/run_full_mission.sh`，结果通过。
- Python 3.8：`compileall` 检查 `mission_lite3`、`tests` 和 `tools/remote_audio_server.py`，结果通过。
- 全量回归：机器狗执行 `python3 -B -m unittest discover -s tests`，共 `396` 项，`396/396` 通过，耗时 `98.332 s`。
- 音频服务现状：`192.168.1.103` 上没有 `lite3-remote-audio.service` systemd 单元；本次已更新工作空间内 `tools/remote_audio_server.py`，但没有安装或启动一个不存在的系统服务。
- 配置边界：当前 `audio.remote_host` 仍是 `192.168.1.120`。因此客户端预热代码已部署，但配置目标上的服务端是否已经支持 `warmup` 本次没有验证；在确认对应音频服务版本前，不能宣称首次播报真机问题已经解决。
- 安全边界：本次没有启动完整任务、RTSP、ROS2、运动控制、机械臂或扬声器播放；远端测试通过仅证明部署文件一致及软件回归通过。

## 记录 22：修复 BOOT_CHECK 首次 RTSP 帧误超时并部署

- 时间：2026-08-07 19:55（当前会话）
- 现场日志：`logs/full_mission/mission_20260807_195110.log`。
- 用户现象：机械臂预检通过后，任务在 BOOT_CHECK 报 `front camera opened but did not deliver a frame`，未进入站立、避障、巡检或抓取。

### 22.1 根因证据

- `19:51:17.514`：主进程创建持续相机读取器。
- `19:51:18.433`：独立采集进程启动，进程创建耗时约 `0.919 s`。
- `19:51:18.434`：OpenCV 开始打开 RTSP。
- `19:51:19.520`：BOOT_CHECK 使用普通 `2.000 s` 读帧超时并中止。
- `19:51:19.640`：RTSP 实际连接成功，比 BOOT_CHECK 截止时间只晚约 `0.120 s`；随后因任务已进入清理而停止，未能发布第一帧。
- 根因：首次读帧把“Python 独立进程启动时间 + RTSP 建链时间”全部计入普通 2 秒运行时超时，现场总耗时约 `2.126 s`，因此把可连接相机误判为无帧。日志中的迟到 `connected` 不是断线，而是清理阶段排出的后台状态。

### 22.2 修复范围

- 修改文件：
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `mission_lite3/mission.py`
  - `tests/test_mission_lite3_smoke.py`
  - `避障集成修改记录.md`
- 新增 `camera.startup_first_frame_timeout_s=6.0`，只供 BOOT_CHECK 等待第一次真实新帧。
- BOOT_CHECK 现在调用 `front_camera.read(timeout_s=6.0)`，并输出 `[camera] boot_first_frame_wait timeout_s=6.000`。
- 普通任务阶段的 `camera.read_timeout_ms=2000` 保持不变；相机在运行中断流时仍会在 2 秒内返回失败，不会把所有读帧都放宽到 6 秒。
- 配置加载阶段要求首次帧等待是 `0.1~30.0 s` 的有限数值，错误配置在任务运动前直接拒绝。
- 未修改相机地址、分辨率、路线、运动参数、避障、识别、抓放动作或机械臂协议。

### 22.3 备份、部署和验证

- 远端备份目录：`/home/ysc/my_sw/.codex_backups/robot_competition_before_boot_camera_timeout_20260807_195517`。
- 备份内容：4 个覆盖前原文件、`before_git_status.txt`、`before_git_diff.patch`、原文件 SHA256 和 `original_files.tar`。
- 备份归档 SHA256：`2c9eb90ee58c4d06bfc494bd5a67cee68b0ec4ddd45712136d6ae4763b28e8f1`。
- 部署记录前的记录 21 版本另存于 `before_record22/避障集成修改记录.md`，SHA256 为 `692b4402ff2671daff4090fb539c9ca0234d3c9e92eeb2bc6173dbb4187dc46a`。
- 4 文件部署包 SHA256：`b0768fe1176ba098df331a830defbb137669fdaeaa05a8f10fa02f41128eec3f`；远端逐文件原子替换结果 `DEPLOY_REPLACED=4`。
- 部署后文件 SHA256：
  - `config_loader.py`：`1fbe5f2234633ecd341234307653ec77ca8bc29b0f526ed41de4b84eb623ee84`
  - `robot.yaml`：`36760b03b96762b39b3dacddae5f118e188c070b917c7572ba2debf050334989`
  - `mission.py`：`e4a2bf81de184fc13a99c52e3b28cd8c49c46398576cf2d9c17ce624f9eda361`
  - `test_mission_lite3_smoke.py`：`0c589b57ec53bec578f20b67686f3680bc4f0ea0e22a0f052a5409c4445eac8a`
- 本地专项测试：3 项全部通过；本地全量回归：`398/398` 通过，耗时 `32.743 s`。
- 机器狗 Python 3.8 专项测试：3 项全部通过，耗时 `3.675 s`；远端全量回归：`398/398` 通过，耗时 `93.100 s`。
- 真机边界：部署和回归期间没有重新启动完整任务或相机。本次软件修复针对已确认的启动竞态，但必须由下一次真实运行日志确认 6 秒窗口内成功收到首帧；不得把单元测试写成真机相机已经通过。

## 记录 23：巡检计数不中断任务、兜底结果重平衡与避障通过时间加倍

- 时间：2026-08-07 20:44（当前会话）。
- 最近完整任务日志：`logs/full_mission/mission_20260807_200331.log`。
- 最近避障时间线：`startup_avoidance_runs/20260807_200339_61e70d1a.jsonl`。
- 用户要求：解决巡检结束后未完成后续流程的问题；避障通过障碍后的前进时间改为原来的 2 倍；业务识别或计数问题不得中断后续抓放流程。
- 修改文件：
  - `mission_lite3/startup_avoidance/controller.py`
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `mission_lite3/mission.py`
  - `tests/test_startup_avoidance.py`
  - `tests/test_mission_lite3_smoke.py`
  - `避障集成修改记录.md`

### 23.1 最近一次任务没有死锁，而是巡检门禁主动结束

- 避障已经正常完成，结果为 `avoidance_count=2`，随后任务完成了四个巡检停靠点并进入 `PICK_RED_BAR`。
- 真实视觉结果为 A 异常，置信度 `0.841`；D 异常，置信度 `0.825`。
- A 位置最初生成的零置信度异常兜底记录在真实 A 到达后被迁移到 B；C 为零置信度正常兜底。最终统计是 3 异常、1 正常，而赛制门禁要求 2 异常、2 正常。
- 原代码因此输出 `block_reason=count_check_fail`，再主动抛出 `PICK_RED_BAR: inspection result does not allow pickup`。进程已正常退出，没有残留任务进程，所以现场看到的“卡死”实际是业务门禁中止，不是线程或 RTSP 死锁。

### 23.2 只重平衡零置信度兜底记录，真实视觉结果保持原样

- 抓取门禁前新增 `_rebalance_default_fallback_records()`。只有同时满足 `source_camera=default_fallback` 和 `confidence=0.0` 的记录允许在已知 2 正常/2 异常约束下重新分配状态。
- 当前日志对应的修复结果是把 B 的零置信度兜底从异常改为正常，A、D 两条真实相机记录不重建、不改状态、不改置信度，最终异常目标为 A、D。
- 被调整的兜底记录保留 `source_camera=default_fallback` 和 `confidence=0.0`，并增加 `stability_votes.fallback_count_rebalanced=1`，避免把兜底结果伪装成真实视觉识别。
- 重平衡后的完整 `round_result.json` 会原子写回，任务日志输出调整区域和目标路径。
- 若真实视觉本身已得到 3 个异常或 3 个正常，代码不会改写真实记录。2/2 门禁此时只输出警告，`PICK_RED_BAR` 和 `SECOND_PICK_PLACE` 仍继续，不再因为计数检查主动中止。
- 若识别结果不足两个异常目标，`_next_target_letter()` 优先从尚未使用的零置信度兜底区域中选择，再按较低置信度和字母顺序选择，保证两轮业务抓放仍有确定目标；日志会明确标记这是 fallback target。
- 边界：急停距离、传感器完全失联、运动控制失败、机械臂串口失败和系统断电/重启仍必须停车或无法继续。禁止为追求“流程不退出”而关闭硬安全停车，否则可能碰撞、摔机或损坏机械臂。

### 23.3 避障 PASS 前进总时间按原来的 2 倍执行

- 最新避障时间线中，第 1 次 `PASS` 从进入到目标清除为 `3.902 s`，前向里程增加约 `0.237 m`；第 2 次为 `4.340 s`，前向里程增加约 `0.263 m`。
- 新增并校验 `startup_avoidance.decision.pass_duration_multiplier=2.0`，允许范围为 `1.0~4.0`。
- 进入 `PASS` 时保存开始时间。当前目标从安全侧离开画面并被跟踪器确认清除后，不立即进入 `RETURN_LINE`，而是把此前已经通过的时长作为额外前进时长；到达扩展截止时间后才回线。
- 因此同一现场节奏下，两次总 `PASS` 时间预计约为 `7.804 s` 和 `8.680 s`，对应前向距离预计约为 `0.474 m` 和 `0.526 m`。实际值会随相机帧周期和里程计反馈产生小量差异。
- 未修改 `pass_vx=0.06 m/s`、`cruise_vx=0.08 m/s`、横移速度、航向保持、`finish_forward_m=2.40 m`、正常巡航路线、抓取动作或放置动作。

### 23.4 本地验证

- 5 个直接相关 Python 文件 AST 解析通过；配置严格加载结果 `pass_duration_multiplier=2.0`。
- 避障专项测试 `14/14` 通过，覆盖 0 个、1 个、多个障碍物、两倍通过时间、回线、结束里程、急停和错误配置。
- 巡检连续性专项测试 `4/4` 通过，覆盖当前 A/D 真实异常场景、3 个真实异常不被篡改、计数门禁警告后继续抓取、无异常时优先选零置信度兜底目标。
- 本地全量回归执行 `py -3.12 -B -m unittest discover -s tests`，共 `403/403` 通过，耗时 `32.933 s`。

### 23.5 机器狗备份、部署和验证

- 部署目标：`ysc@192.168.1.103:/home/ysc/my_sw/robot_competition`；部署前没有完整任务进程或避障任务进程运行。
- 覆盖前备份目录：`/home/ysc/my_sw/.codex_backups/robot_competition_before_pass2_gate_continue_20260807_202933`。
- 备份包含 6 个覆盖前原文件、`targets.txt`、`before_git_status.txt`、`before_git_diff.patch`、`original_sha256.txt`、`original_files.tar` 及其 SHA256 文件。
- 备份归档 SHA256：`27330d0eb215bc6a827c94b28426c68eacfdb1e118c7f99415108d2ff4645139`。
- 6 文件部署包 SHA256：`d5c78a59dd9ea997356c0a1019209b8e7d8d33ecfc67e659350fb69ff4e84c75`；暂存区逐项验证通过后原子替换，结果 `DEPLOY_REPLACED=6`。
- 部署后 SHA256：
  - `controller.py`：`e6a7af6759d391383d56b0e35eca189cc8fe02f1a60a32a08495347a1d265175`
  - `config_loader.py`：`0e8515285d52dc536ba1f2f1ddaef59d338c439aeb9bce541a29443b780edec2`
  - `robot.yaml`：`51d5da628854c2c04c43deb7105e5ee065776620eaa970a38f08a3bcc729556c`
  - `mission.py`：`647a426430864e59af1388bef0692b6111a982b6147707d4db5809dd07bf8b0e`
  - `test_startup_avoidance.py`：`fb139b171a93d26d0437e16a448a3f35bb0882b3dc0b8718647dd2ce61609b34`
  - `test_mission_lite3_smoke.py`：`a338634e7301469614616897366e041bf648cf04eb36a74d1c39a187bf9ce2b0`
- 机器狗 Python 3.8 `compileall` 通过；严格配置输出 `REMOTE_CONFIG_MULTIPLIER 2.0`；远端专项测试 `18/18` 通过，耗时 `7.905 s`。
- 首次远端全量回归期间 SSH 在约 63 项后断开。重新连接发现机器狗 20:39 整机重启，`uptime` 只有 1 分钟，`last -x` 把上一会话标为 `crash`；系统没有持久 journal，因此没有足够证据判断是供电、内核还是外部重启，也不能归因于单元测试。
- 重启后再次核对 6 个文件 SHA256，全部与部署值一致。随后用低优先级、断线可恢复的后台日志方式重新执行全量回归，结果 `403/403` 通过，耗时 `106.253 s`，退出码 `0`，日志保存在备份目录的 `remote_full_unittest.log`。
- 本次没有启动 `run_full_mission.sh`，没有发送 ROS2/UDP 运动、机械臂、相机或音频命令。软件测试不能代替真实场地验证；下一次真机运行必须重点检查 `pass_extension_start -> pass_extended -> return_start` 的时间线、A/D 目标选择、两次抓放完成情况和系统是否再次异常重启。

## 记录 24：避障 PASS 速度提高 1.7 倍并强制先接近箱区到 0.8 米

- 时间：2026-08-07 21:30（当前会话）。
- 最近完整任务日志：`logs/full_mission/mission_20260807_205155.log`。
- 最近避障时间线：`startup_avoidance_runs/20260807_205204_7b1d627f.jsonl`。
- 最近放置导航时间线：`placement_letter_navigation_runs/20260807_205740_594329_0ba3c2da/events.jsonl`。
- 用户要求：避障通过阶段的前进速度调整为原来的 `1.7` 倍；抓住红色方块后必须先前进，使箱区与机器狗距离达到 `0.8 m`，再横移寻找目标字母并放置；解决本轮再次未完成全流程的问题。
- 修改文件：
  - `mission_lite3/placement_letter_navigation.py`
  - `mission_lite3/mission.py`
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `tests/test_placement_letter_navigation.py`
  - `tests/test_box_center_mission.py`
  - `tests/test_startup_avoidance.py`
  - `tests/test_mission_lite3_smoke.py`
  - `避障集成修改记录.md`

### 24.1 最近一次任务不是线程死锁，而是远距离横移后主动超时

- 两次避障已完成，红色方块 B 第一次抓取失败后第二次成功；机械臂返回 `ok=true`、`object_held=true`，随后任务进入 B 箱放置导航。
- 放置导航开始时前方超声距离约为 `2.24 m`。相机能够识别远处的 A/B 箱，但旧决策把“已经看到任意字母”当作结束前进搜索的条件，因此没有先接近箱区，直接在约 `2.24 m` 处左右横移。
- 90 秒内横移累计请求约 `1.027 m`，但远处 A/B 的图像位置几乎不变；最后事件为 `placement letter navigation failed: total_timeout`，时间线记录 `elapsed_s=90.25932281399992`。
- 整个失败阶段相机保持在线并持续产生新帧，机械臂也已经确认持有红块；现场看到的“卡住”是放置导航在错误距离反复横移后主动超时，不是 RTSP、机械臂线程或任务进程死锁。任务退出后没有残留完整任务进程。

### 24.2 避障 PASS 速度只提高到 0.102 m/s

- 最近避障 JSONL 证明 PASS 阶段实际下发值一直是 `vx=0.06 m/s`。
- `startup_avoidance.speed.pass_vx` 从 `0.06` 改为 `0.102 m/s`，计算为 `0.06 * 1.7`。
- 记录 23 的 `pass_duration_multiplier=2.0` 保持不变，因此仍在目标清除后继续同样长的 PASS 时间；本次只提高该阶段的前进速度。
- `cruise_vx=0.08 m/s`、`avoid_vy=0.08 m/s`、航向控制、`finish_forward_m=2.40 m`、普通路线、抓取和放置动作速度均未修改。

### 24.3 放置改为“先接近 0.8 米，再横移识别，再放置”

- `placement_letter_navigation.front_stop_distance_m` 从 `0.35` 改为 `0.80`，该值现在是进入横移字母搜索阶段的距离门槛；全局硬安全配置 `safety.front_stop_distance_m=0.35` 保持不变。
- `PlacementLetterNavigator.decide()` 在处理任何 A/B/C/D 候选框之前先检查超声距离。只要距离大于 `0.80 m`，即使已经看见目标字母或其他字母，也只能返回纯前进动作 `approach_to_lateral_search_distance`，不能提前横移或完成。
- `_run_placement_forward_search_step()` 不再因“看见任意字母”提前结束前进步；它持续读取相机、超声和里程计，直到本步前进距离完成或超声距离达到 `0.80 m`，随后停车并由下一帧开始横移定位。
- 前进预算从 `1.38 m` 增至 `1.80 m`，覆盖本次从约 `2.24 m` 接近到 `0.80 m` 所需的约 `1.44 m`，并保留预算上限。
- 每次前进采样后立即检查机体坐标系前向里程；若前进命令对应的实测位移为负，立即停车并报告 `placement forward odometry moved backward`，不再多等一帧后被相机错误覆盖真实原因。
- 横移速度仍为 `0.08 m/s`，单步横移、最大横移预算、连续 3 帧居中确认、机械臂放置动作和物理左右方向映射均未修改。

### 24.4 本地验证

- Python 3.12 专项回归：放置导航、任务放置执行器、避障和配置共 `191/191` 通过，耗时 `12.616 s`。
- 新增现场回归覆盖：在 `2.24 m` 已看见 A/D 时仍只能前进；到 `0.80 m` 后才首次允许横移；前进过程中看见字母不能提前截断；里程反向时立即停车并保留真实错误原因。
- 本地全量回归执行 `py -3.12 -B -m unittest discover -s tests`，共 `405/405` 通过，耗时 `33.505 s`。

### 24.5 机器狗备份、部署和验证

- 部署目标：`ysc@192.168.1.103:/home/ysc/my_sw/robot_competition`；部署前和验证后均没有 `mission_lite3.run_mission` 或 `run_full_mission.sh` 进程。
- 8 个代码、配置和测试文件的覆盖前备份目录：`/home/ysc/my_sw/.codex_backups/robot_competition_before_pass17_place080_20260807_212418`。
- 备份包含原文件、`manifest.txt`、`before.sha256`、覆盖前 Git 状态、二进制差异和 `originals.tar.gz`；备份文件数为 `8`，归档 SHA256 为 `a7d4c1e654ea513d3904b647defc52666dbdd6c4ffbfabc019eaaaa64f384174`。
- 部署包 `lite3_pass17_place080_20260807_212418.tar` 的本地与远端 SHA256 均为 `07eb0a38579f76bdae2b9a06bb86d6c59190a569e553703525289f9b39bf23e6`；成员清单和暂存区逐文件哈希全部通过后，才在目标目录逐文件原子替换。
- 记录 23 原件另存于上述备份目录的 `before_record24/避障集成修改记录.md`，原 SHA256 为 `59e73bfb8c62d3491656c9973ed6ed08c477ccd25f74ca02f5e238277f830919`，记录归档 SHA256 为 `d6dcc8f7900e377c26643b72ea06b64d8ef189fab1d4e91282798e4d43f0dc0e`。
- 部署后 SHA256：
  - `placement_letter_navigation.py`：`c619b52a68328a9f2e0815be55409986bdaeba1ef108f26a211e3d62b84966da`
  - `mission.py`：`3717c806a48071dce5c384bdc7f25cac138cf6eb617d20d4bd03e8ea239e1b7c`
  - `config_loader.py`：`ecd449e8b3ef9edbec2fda5603579e3b08655de886c8ec471d3d14cdc76fcbbe`
  - `robot.yaml`：`0dc1e522d48e2506e99d414c3f57096e57c42c88add4c59b995f4b7cfc268ebb`
  - `test_placement_letter_navigation.py`：`59cd2e8d56a40d1694f0316aefd020aab155fe04c3d35d0f2e3eeaf1482ea9f7`
  - `test_box_center_mission.py`：`ecf9e6e62d31407e59a3778b3d255cba563f05ba9bc15a73f31ab33fc0ac752a`
  - `test_startup_avoidance.py`：`288a343cbccc8fb28e78c098a6c9a5b5d12693316fecde0a36b37a19a2ac1311`
  - `test_mission_lite3_smoke.py`：`3b7ff1f09e2ea7c24a58dcc12d64abb40c4c8d9ee482138a890ad7c7f34aca59`
- 机器狗 Python 3.8 专项回归 `191/191` 通过，耗时 `53.907 s`；低优先级、日志落盘的远端全量回归 `405/405` 通过，耗时 `93.432 s`，退出码 `0`，没有 `FAIL` 或 `ERROR`。

### 24.6 真机待验证边界

- 本批部署和测试没有启动 `run_full_mission.sh`，没有打开 RTSP、发送 ROS2/UDP 运动命令或操作机械臂。
- 单元测试与文件哈希只证明配置、状态机和部署内容在当前测试覆盖内正确，不能证明实际 PASS 速度已经达到 `0.102 m/s`、机器狗真实停在 `0.80 m`、横移能找到目标字母或两次放置能够完成。
- 下一次完整真机运行必须检查最新避障 JSONL 中 PASS 的实际 `vx`，并检查放置导航时间线是否严格呈现“距离大于 `0.80 m` 时只有 forward -> 到达 `0.80 m` 后首次 strafe -> 连续居中 -> place”。若仍失败，应保留新日志继续定位，不得把本次软件回归写成真机流程已经成功。

## 记录 25：避障 20 厘米触发、2 米结束并取消放置前进的后退里程中止

- 时间：2026-08-08 14:25（当前会话）。
- 最近完整任务日志：`logs/full_mission/mission_20260808_134640.log`。
- 最近避障时间线：`startup_avoidance_runs/20260808_134649_b007fc33.jsonl`。
- 最近放置导航时间线：`placement_letter_navigation_runs/20260808_135303_920737_b2e7190a/events.jsonl`。
- 用户要求：正前方和侧方均改为距离障碍物 `0.20 m` 才开始避障，避障直行结束里程改为 `2.00 m`；修复抓取后在放置阶段停止的问题，并完全取消该阶段的后退里程安全中止。
- 修改文件：
  - `mission_lite3/placement_letter_navigation.py`
  - `mission_lite3/mission.py`
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `mission_lite3/README.md`
  - `tests/test_placement_letter_navigation.py`
  - `tests/test_box_center_mission.py`
  - `tests/test_startup_avoidance.py`
  - `tests/test_mission_lite3_smoke.py`
  - `避障集成修改记录.md`

### 25.1 最新任务不是死锁，而是放置前进的负向里程判断误触发

- 旧配置仍为正前方 `0.35 m`、侧方 `0.50 m`、紧急停车 `0.20 m` 和结束里程 `2.40 m`。本轮第一个障碍在约 `0.493 m` 触发，第二个在约 `0.286 m` 触发，避障最终在前向里程约 `2.4025 m` 结束。
- 随后红色方块抓取成功，任务进入目标箱放置导航。失败原因为 `placement forward odometry moved backward`，不是相机、机械臂线程或主进程死锁。
- `_run_placement_forward_search_step()` 先保存起点里程，再采集和处理相机帧。旧判断在第一次 `motion.move()` 尚未下发时就比较里程，采帧期间约 `-0.02 m` 的静止抖动被误判成真实倒退，于是主动停车并退出。

### 25.2 避障改为 20 厘米触发并在 2 米结束

- `startup_avoidance.distance.front_trigger_m` 从 `0.35` 改为 `0.20 m`。
- `startup_avoidance.distance.side_trigger_m` 从 `0.50` 改为 `0.20 m`。
- 原紧急停车线也是 `0.20 m`，会与新触发线重合并导致刚触发就硬停，因此 `emergency_stop_m` 同步改为 `0.15 m`，保留不可绕过的近距离硬停车边界。
- `startup_avoidance.decision.finish_forward_m` 从 `2.40` 改为 `2.00 m`。结束条件仍要求当前绕行和回线完成，不会在正在侧移绕障时仅因达到里程立即结束。
- `pass_vx=0.102 m/s`、`pass_duration_multiplier=2.0`、航向保持和障碍跟踪逻辑保持记录 24 的部署值。

### 25.3 完全取消放置前进的后退里程安全中止

- 删除 `_run_placement_forward_search_step()` 中 `placement forward odometry moved backward` 的检查和异常路径，无论负向里程出现在前进命令之前还是之后，都不会再因此停车退出。
- `PlacementLetterNavigator.record_motion()` 对任意负向前进测量统一按 `0 m` 进度处理，不累计为前进距离，也不再抛出 `forward action moved backward`。
- 保留前方超声停止距离、超声/里程计新鲜度、传感器失联、姿态越限、运动接口异常、单步距离预算、总前进预算和总超时等其他安全边界。本次没有取消横移方向错误检测。

### 25.4 本地验证

- Python 3.12 `compileall` 通过；严格配置加载结果为前方触发 `0.20 m`、侧方触发 `0.20 m`、紧急停车 `0.15 m`、结束里程 `2.00 m`。
- 避障、放置导航、任务放置执行器和配置专项回归 `192/192` 通过，新增覆盖前进命令下发后里程持续短暂为负但任务不退出、最终正向进度正常记录。
- 本地全量回归执行 `py -3.12 -B -m unittest discover -s tests`，共 `406/406` 通过，耗时 `34.441 s`。

### 25.5 机器狗备份、部署和验证

- 部署目标：`ysc@192.168.1.103:/home/ysc/my_sw/robot_competition`；部署前和验证后均没有 `mission_lite3.run_mission`、`run_full_mission.sh`、避障任务或测试进程。
- 9 个代码、配置、说明和测试文件的覆盖前备份目录：`/home/ysc/my_sw/.codex_backups/robot_competition_before_avoid020_finish200_no_reverse_abort_20260808_141739`。
- 备份包含原文件清单、覆盖前 SHA256、Git 状态与二进制差异以及 `originals.tar.gz`；原文件归档 SHA256 为 `71b1c421462cf6d1954d7b04e825ed6a0dd77c7d4d252adc9cdb01b149b8f663`。
- 9 文件部署包本地和远端 SHA256 均为 `4382ccfacb596e68f9c817e1c01619ced5b19c4ebe62196c6006c405fee20c5e`；包成员、暂存区逐文件哈希和替换后逐文件哈希全部通过，结果 `DEPLOY_REPLACED=9`。
- 部署后 SHA256：
  - `placement_letter_navigation.py`：`f62173af3ce163f16fb1fd5bba76e331537f5338a8f93e5eefbf8107a2a3ece1`
  - `mission.py`：`ed9e301e83cd704160f991b9a9097dfc07bdebbc609214656a1c229f1dd596b2`
  - `config_loader.py`：`b3c686380e6a4b1a2284ce39c44d23fcc585694c3f354afae459cc33913f723e`
  - `robot.yaml`：`937dbc4971853d7fbda40be79e8dbf63165a708cd522641fe24203be518bb911`
  - `README.md`：`c4cf24985328d48b8fd27d93ae5c97aeebb638466e153adb45745572110fb7e7`
  - `test_placement_letter_navigation.py`：`d725e091536f349b6b6fe9ef47aa2435b2ffeea8e2db5ad373e8bf2f7f670185`
  - `test_box_center_mission.py`：`ebf61e2d2b22d59680bd4f90a1dea9c65919d1154c332eea919999318943c9b1`
  - `test_startup_avoidance.py`：`082506e28110de597ad83e649d62d795d87ba1a9b99ab06872d75641eddb70ed`
  - `test_mission_lite3_smoke.py`：`289ad48567cefedf372d16201c39052339347b547c99abb77923f53e99fd4e71`
- 机器狗 Python 3.8 `compileall` 和严格配置加载通过；专项回归 `192/192` 通过，耗时 `53.834 s`；远端全量回归 `406/406` 通过，耗时 `100.043 s`，退出码 `0`，日志保存在本批备份目录的 `remote_full_unittest.log`。

### 25.6 真机待验证边界

- 本批部署和测试没有启动 `run_full_mission.sh`，没有打开 RTSP、发送 ROS2/UDP 运动命令或操作机械臂。
- 软件回归证明新参数、状态机和取消后退中止逻辑在测试覆盖内正确，但不能证明真机一定在精确 `0.20 m` 触发、恰好 `2.00 m` 结束或两次抓放已完成。
- 下一次完整真机运行必须检查新避障 JSONL 的两次实际触发距离和最终前向里程，并检查放置导航在短暂负向里程后继续接近 `0.80 m`、横移对准、放置。若仍停止，应以新的终止事件继续定位，不能仅凭本次单元测试宣称全流程实机通过。

## 记录 26：放置前进的负向里程改为按绝对值计入

- 时间：2026-08-08 14:42（当前会话）。
- 用户澄清：放置阶段不能把负向里程丢弃为 `0 m`；里程计返回负值时仍要保持正向前进命令，不报错、不停车，并把该位移计入前进距离。避障仍要求前方和侧方均在 `0.20 m` 才触发，累计前进 `2.00 m` 结束。
- 本次只修改：
  - `mission_lite3/placement_letter_navigation.py`
  - `mission_lite3/mission.py`
  - `tests/test_placement_letter_navigation.py`
  - `tests/test_box_center_mission.py`
  - `避障集成修改记录.md`

### 26.1 行为修正

- `_run_placement_forward_search_step()` 对机体前向投影结果取绝对值后参与本步距离判断。正值和负值都表示已产生的前进位移量，达到本步距离后正常停车并进入下一次视觉决策。
- `PlacementLetterNavigator.record_motion()` 同样使用实测值的绝对值累计 `forward_travel_m`，因此负向里程会进入总前进预算，不会被丢弃，也不会触发后退错误。
- 底盘命令保持 `vx=placement_letter_navigation.forward_speed_mps`、`vy=0`、`wz=0`，即始终要求机器狗向前走；没有因为里程符号改变运动方向。
- 前方超声停止距离、数据新鲜度、运动接口异常、单步距离上限、总前进预算和总超时仍保留。
- 避障配置没有再次改动，严格加载值仍为前方触发 `0.20 m`、侧方触发 `0.20 m`、紧急停车 `0.15 m`、结束里程 `2.00 m`。

### 26.2 验证与部署

- 本地 Python 3.12 直接相关测试 `68/68`、四模块专项 `192/192`、全量 `406/406` 通过；全量耗时 `35.066 s`。
- 新增回归使用全程负向里程 `0 -> -0.02 -> -0.10 m`：任务保持下发正向 `vx=0.08 m/s`，绝对位移达到 `0.10 m` 后正常结束本步，导航器累计前进 `0.10 m`。
- 机器狗覆盖前备份：`/home/ysc/my_sw/.codex_backups/robot_competition_before_count_reverse_odom_20260808_143745`。
- 4 文件原件归档 SHA256：`898759962fbd9e62a809b909730e560a2fb66632fca64a6ec33e2db24e012d3f`。
- 4 文件部署包 SHA256：`e74bb6bbf4fa52435e53fe83aef6de7865994e5acf0363b3d2086dd98a150e4a`；部署结果 `DEPLOY_REPLACED=4`。
- 部署后 SHA256：
  - `placement_letter_navigation.py`：`d4e3dbc60efb3b1c62e5c593f4f3e09627f68ee2082f0d0042047bf239a87d2b`
  - `mission.py`：`7ba17662923ca78740e2aa8dcdf2ce497299baea85ef676479d07964445d9642`
  - `test_placement_letter_navigation.py`：`7878c635d4b631bfbeb9121ead10cfefd4deab7929b2172c29cae56ca19a6d60`
  - `test_box_center_mission.py`：`3d133fd4a035a6d59d5a8aca987b5603411814e170bfe31b671b6094ef23cd02`
- 机器狗 Python 3.8 专项 `192/192` 通过，耗时 `52.320 s`；全量 `406/406` 通过，耗时 `92.910 s`，退出码 `0`，日志位于本批备份目录的 `remote_full_unittest.log`。
- 部署和测试期间没有启动完整任务、相机、运动或机械臂；实际运动方向、`0.20 m` 触发距离和 `2.00 m` 结束里程仍需下一次真机完整运行日志确认。

## 记录 27：开场 0.40 米避障与抓取路线前方边界确认续跑

- 时间：2026-08-08 15:20（当前会话）。
- 最近完整任务日志：`logs/full_mission/mission_20260808_145553.log`。
- 最近避障时间线：`startup_avoidance_runs/20260808_145607_015f3aa7.jsonl`。
- 用户要求：开场改为距离障碍物 `0.40 m` 开始避障，避障仅限起点到第一个仪表盘之前且仍在前向里程 `2.00 m` 结束；找出本轮中途停止根因并避免可恢复业务情况结束整项任务。
- 修改文件：
  - `mission_lite3/mission.py`
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `mission_lite3/config/field.yaml`
  - `mission_lite3/README.md`
  - `tests/test_box_center_mission.py`
  - `tests/test_mission_lite3_smoke.py`
  - `tests/test_startup_avoidance.py`
  - `避障集成修改记录.md`

### 27.1 本轮不是死锁，根因是抓取路线把正常到达前方边界当成致命碰撞

- 开场避障产生 `536` 条决策，跟踪到目标的帧数为 `344`，超声最小值约 `0.28 m`。旧触发线为 `0.20 m`，所以所有非终止决策均为 `cruise`，没有进入绕行动作，`avoidance_count=0`。
- 避障在前向投影里程 `2.000955 m` 正常输出 `FINISHED`；随后四个仪表盘巡检全部完成，记录顺序为 B、A、D、C。音频服务的预热和增益告警没有中止状态机。
- 失败发生在 `PICK_RED_BAR` 的 `pickup_from_upper_inspection`：转向后准备向抓取区前进 `1.35 m`，通用运动保护读到 `ultrasound=0.28 m`，低于全局 `0.35 m` 停止线，抛出 `motion guard stopped forward command`。
- 主任务随后按设计清理相机并以 `exit_status=1` 退出，最终状态为 `ABORT_SAFE`，没有残留完整任务进程。因此现场看到的停止是状态机主动失败，不是线程、RTSP、机械臂或操作系统卡死。
- 结构性根因：固定距离抓取路线没有“已到达抓取区前方边界”的完成语义，任何正向运动保护触发都只能作为致命异常传播；同样问题还可能在第一次投放后返回第二次抓取的前进段重复出现。

### 27.2 `0.40 m` 避障只作用于第一个仪表盘之前

- `startup_avoidance.distance.front_trigger_m` 和 `side_trigger_m` 均从 `0.20` 改为 `0.40 m`；`emergency_stop_m=0.15 m` 和 `finish_forward_m=2.00 m` 保持不变。
- 该控制器只在任务状态 `PASS_OBSTACLE` 中创建和运行。达到 `2.00 m` 且当前绕行、回线已完成后输出零速度并返回，下一状态为 `INSPECT_LEFT_OBJECT`。
- 第一个仪表盘及后续巡检、抓取、放置阶段不会再次运行 `startup_avoidance`，也不会使用 `0.40 m` 触发侧移绕障。

### 27.3 两条抓取往返路线确认到达后继续，不关闭全局防撞

- 新增专用 `ForwardMotionGuardStop`，只表示“有效前方距离保护停止了正向命令”，不再通过错误文本猜测异常类型。
- 路线前进动作新增严格布尔字段 `front_stop_is_completion`，且只允许用于 `forward`。当前仅在 `pickup_from_upper_inspection` 和 `pickup_from_place` 的前进段启用，覆盖首次和第二次返回抓取区。
- 带该标记的前进段遇到运动保护后立即保持零速度，并要求连续 `3` 个新鲜超声样本确认距离仍不大于该阶段停止线；确认后将本前进段视为正常完成，继续执行横移进入抓取通道。
- 若只是单帧超声尖峰，程序扣除已经发生的里程后恢复剩余前进距离；恢复总窗口为 `90 s`，不会因单次误触发结束整项任务。
- 未标记的普通前进路线仍会在前方保护触发时中止；`0.15 m` 紧急距离、传感器失联、姿态越限、运动接口异常和系统断电不允许强行续跑。无法安全实现“任何硬件故障都不停”，本次保证的是可恢复业务/路线到达事件不再误结束任务。

### 27.4 本地与机器狗验证

- 本地 Python 3.12 `compileall` 通过，严格配置输出 `0.40 / 0.40 / 2.00`，两条抓取路线的 `front_stop_is_completion` 均为 `true`。
- 本地专项回归 `223/223` 通过，覆盖 `0.40 m` 边界、抓取路线确认前方停止后继续、放置路线同类行为、未标记普通路线仍中止、字段类型和动作类型校验；本地全量 `408/408` 通过，耗时 `34.057 s`。
- 机器狗覆盖前备份：`/home/ysc/my_sw/.codex_backups/robot_competition_before_avoid040_pickup_front_completion_20260808_151520`；其中 `diagnosis/` 保存本轮主日志和避障 JSONL。
- 8 文件原件归档 SHA256：`09a8e8e904414c7ada904481a544c2080b2bbdf8ac60990fbceb002c5b9d14c9`。
- 8 文件部署包 SHA256：`f491f027cc600685d5392959c5fa8b6a1a722eb6099ab60d2ebff1d3a1788b37`；部署结果 `DEPLOY_REPLACED=8`。
- 部署后 SHA256：
  - `mission.py`：`a937e5bcff2481e66427ba9e5f45e9b4b56e1020ef8a596c7aaaaf61d0932049`
  - `config_loader.py`：`3c0fdddb408cc500f6289b477a85130c9994a0ab0137ccbe2a94f2e05dc53174`
  - `robot.yaml`：`ebcb8f723354fd41ab979afef613130317e80d2812d0a4bc6b694957e9d8aaac`
  - `field.yaml`：`5531f724f5323cf89127d9c32ef522ee77676faf8e82f1e9444166ceba89cb3f`
  - `README.md`：`d0e17751387d19c1c6eec086e5dff2814aea893a7fe8cad8d9eaa83bc726c3ee`
  - `test_box_center_mission.py`：`02336c8ea36516d8f518ad930460637592b8907fb675ca0838576611f24edf03`
  - `test_mission_lite3_smoke.py`：`8cec22534cf27761bce4862b7690f2e0d6be88b58c5660d353ceee81ea360744`
  - `test_startup_avoidance.py`：`986f07894778ee9970de2573144d4c8e7071c9e7bc6788eb709aa1a16cdb176a`
- 机器狗 Python 3.8 专项 `223/223` 通过，耗时 `79.887 s`；全量 `408/408` 通过，耗时 `91.517 s`，退出码 `0`，日志保存在本批备份目录的 `remote_full_unittest.log`。
- 本批部署和测试没有重新启动完整任务、相机、运动或机械臂。下一次真机运行必须确认避障在约 `0.40 m` 进入 `CONFIRM`，`2.00 m` 后进入第一个巡检点，并在抓取路线出现连续近距离时记录 `pickup route forward reached confirmed ultrasound stop distance` 后继续横移和抓放。

## 记录 28：开场避障嵌套重计 2 米与整任务故障保持

- 时间：2026-08-08 16:40（当前会话）。
- 用户最终确认的边界：任务不能因可恢复的软件、传感器或业务异常中途退出；异常时保持进程存活并输出零速度，安全恢复后从受控检查点继续。不得在传感器或位姿无效时盲目运动，也不实现故意撞障碍物。巡检移动、抓取和放置需要人工确认，避免重复执行物理动作。
- 开场避障参数保持正前方/侧方 `0.40 m` 触发、`0.15 m` 紧急保持线、每轮横移后实际前进 `2.00 m`。避障控制器只在 `PASS_OBSTACLE`、即起点到第一个仪表盘之前运行。
- 修改文件：
  - `mission_lite3/startup_avoidance/controller.py`
  - `mission_lite3/startup_avoidance/runtime.py`
  - `mission_lite3/mission.py`
  - `mission_lite3/config_loader.py`
  - `mission_lite3/config/robot.yaml`
  - `mission_lite3/README.md`
  - `README.md`
  - `tests/test_startup_avoidance.py`
  - `tests/test_mission_lite3_smoke.py`
  - `避障集成修改记录.md`

### 28.1 避障距离改为真实里程并支持连续障碍

- 删除 PASS 阶段按时间结束的逻辑。第一次横移完成后设置独立的 `pass_origin`，只有 `/leg_odom2` 在当前前进轴上的投影达到 `finish_forward_m=2.00 m` 才允许回线。
- 如果本轮 2 米前进中检测到第二个障碍，先重新确认并再次向左横移；前一轮 `pass_progress_m` 清零，以最新横移完成点作为新起点重新走满 2 米。
- 第一次绕障前的原始横向线始终保留。所有连续障碍完成后只回线一次，不会把第二次横移位置误当成原车道。
- 第二个障碍在恰好达到 2 米的同一帧出现时，障碍确认优先于回线，避免刚到阈值就遗漏新障碍。

### 28.2 避障故障保持

- `0.15 m` 紧急距离、图像/超声/里程计过期或无效、红色跟踪歧义、近距离未知目标和回线数据无效均进入控制器 `HOLD`，不再返回终止型 `FAULT`。
- `HOLD` 持续输出零速度并保留当前状态、当前 PASS 起点与进度、第一次原始回线和避障次数。连续 5 个有效帧后恢复保存的状态。
- 相机打不开/读取失败、运动接口瞬时异常和运行循环异常由 `StartupAvoidanceRunner` 在同一进程中持续停止、释放相机、等待并重连。正式模式无重试次数上限；测试注入的 `max_hold_retries` 只用于防止单元测试无限等待。

### 28.3 主任务 `FAULT_HOLD`

- 新增 `MissionState.FAULT_HOLD`。机器人状态、姿态、电量、数据新鲜度、运动接口、相机、机械臂、抓取和放置异常不再直接进入 `ABORT_SAFE`；进程持续尝试发送零速度并检查恢复条件。
- `BOOT_CHECK`、`STAND_AND_ARM`、`PASS_OBSTACLE`、`REPORT_RESULTS` 和 `FINISH_OR_SAFE_STOP` 在安全状态连续稳定 5 次后自动重试。
- 两段巡检、首次抓取、首次放置和第二轮抓放不盲目重放。操作员确认机器人与夹爪状态后执行 `touch /tmp/lite3_fault_resume`；信号在数据尚未恢复时会锁存，安全状态连续稳定后才恢复。
- 修复最终第二轮投放重试的重复确认：一次失败只消费一个新的恢复信号，不会先在异常处理处消费一次、回到最终检查后又要求第二次。
- 可恢复的 `_check_safety()` 不再发送可能锁止底盘的 `soft_estop`，改为零速度停止后进入 `FAULT_HOLD`。人工/硬件急停仍由现场人员保留。
- dry-run 保持有限失败语义，单元测试不会无限等待。`Ctrl+C`、进程被操作系统终止、主机掉电或操作系统故障仍可能终止任务。

### 28.4 配置与回归验证

- 新增并严格校验：`fault_hold.enabled=true`、`poll_interval_s=0.50`、`recovery_stable_checks=5`、`resume_signal_path=/tmp/lite3_fault_resume`、`startup_avoidance.fault_hold_retry_s=0.50`。
- 本机 Python 3.8 `compileall` 通过；Python 3.8 环境未安装 `cv2`，因此本地行为测试使用已配置 OpenCV 的 Python 3.12。新增配置、单次恢复信号和非锁止停车回归 `4/4` 通过。
- 避障、主任务、放置导航和抓取转运专项 `234/234` 通过；本地全量 `419/419` 通过，耗时 `34.348 s`。
- 全量测试与静态检查证明当前测试覆盖内状态机可恢复，不能代替真机运动、相机、传感器、机械臂和断电场景验证。

### 28.5 待部署包

- 9 文件部署包：`I:\tmp\lite3_fault_hold_recovery_20260808_163744.tar`，SHA256 为 `45ba9f4c0322e7b597ead2ed8aca21b1b12c696a5defcc4cb9b4633241151ac7`；本地解包后逐文件哈希 `9/9` 一致。
- 原子部署脚本：`I:\tmp\deploy_lite3_fault_hold_recovery_20260808_163744.sh`，SHA256 为 `896536ba89494b91fed39dff3470ef15126efab820d25ad1f4240b2bda57472f`。脚本会阻止任务/测试进程并发部署、备份最新任务和避障日志、备份 9 个远端原文件、校验包与成员哈希、逐文件原子替换，并在 Python 3.8 编译、配置断言、专项或全量测试失败时自动回滚。
- 部署前状态：当时机器人 SSH 缺少密码，因此本节生成包时尚未部署；实际部署结果见 28.7。部署过程先执行远端 `bash -n`，完成后没有自动启动完整任务。

### 28.6 旧版 16:47 实机日志的精确根因

- 部署前最新完整任务日志为 `logs/full_mission/mission_20260808_164702.log`，对应避障日志为 `startup_avoidance_runs/20260808_164711_485f3238.jsonl`；两者已保存到本地 `I:\tmp\lite3_diagnosis_20260808_164702` 和远端部署前备份的 `diagnosis/`。
- 旧避障控制器在 `time=7049.013` 进入 PASS，前向总投影为 `0.5483 m`；在 `time=7053.625` 结束 PASS，前向总投影为 `1.0253 m`。横移后的直行只持续约 `4.61 s`、前进约 `0.477 m`，随后立即进入 RETURN_LINE，并未从横移完成点前进 2 米。
- 旧控制器回线后重新进入普通 CRUISE，最后才把从任务起点计算的总前向投影补到 `2.0013 m`。因此“整个开场累计 2 米”和“横移结束后直行 2 米”被错误地当成同一结束条件，现场看到的绕障直行明显过短。
- 最终停止不是线程死锁。任务在 `PICK_RED_BAR` 的 `pickup_from_upper_inspection` 前进段读到 `ultrasound=0.31 m`，低于通用 `0.35 m` 阈值，抛出 `motion guard stopped forward command`；旧主状态机随后进入 `ABORT_SAFE`，结果 `exit_status=1`，进程正常退出。

### 28.7 机器狗部署与远端验证

- 部署时间：2026-08-08 16:56（机器人 CST）。部署目标为 `ysc@192.168.1.103:/home/ysc/my_sw/robot_competition`。
- 远端脚本上传后先通过 `bash -n`；部署包远端 SHA256 与本地一致，为 `45ba9f4c0322e7b597ead2ed8aca21b1b12c696a5defcc4cb9b4633241151ac7`。
- 覆盖前备份：`/home/ysc/my_sw/.codex_backups/robot_competition_before_fault_hold_recovery_20260808_163744`。9 个原文件归档 SHA256 为 `9a2071c1787217a8a59b8da337e044b4f88bd420ee5c1f69c9d8ef6a8106edd6`；备份同时保存覆盖前哈希、Git 状态、二进制差异和最新实机日志。
- 原子替换结果 `DEPLOY_REPLACED=9`。部署后逐文件 SHA256：
  - `startup_avoidance/controller.py`：`a4a260f692be24400a4cd88d165f801db7aa2c93019a4d8843acad64686c808b`
  - `startup_avoidance/runtime.py`：`bc65888e27406c15d87dcdeb49a145bbd15be9ded78581e605a38a27318cf497`
  - `mission.py`：`5a7c4df29c0f698c172332992702d2815a90b6351f740040c45a847060366ad9`
  - `config_loader.py`：`cfe4f0120e798a8f2f881f01a188a3fc81e17e387f386fbc7d236cc03d1172f1`
  - `config/robot.yaml`：`7afb5a79dc259283ed79e6947f63e785090727139868b2934baeaf46ea63cdb1`
  - `mission_lite3/README.md`：`d6bbd9d609ecdbee86449f1e96d60f5d1d0080a592df6ca398bef3e3b29ee696`
  - `README.md`：`8e35655ec3ed447823284405d54cc53ee3f5b347801498b1adfd5479680db700`
  - `tests/test_startup_avoidance.py`：`6b18844e0a0aff69b4b75a8c38889ed2abc172451d905db5c3603f86f6616fee`
  - `tests/test_mission_lite3_smoke.py`：`79266df2cce71b04e03a1eca1841202348565b6b9f59cfc4f1ae36f64d490851`
- 机器人 Python `3.8.10` 编译和严格配置断言通过，输出 `REMOTE_CONFIG_OK 0.4 2.0 5`。避障与主任务专项 `137/137` 通过，耗时 `53.743 s`；远端全量 `419/419` 通过，耗时 `103.531 s`。
- 部署前、部署后均无完整任务、避障或测试残留进程，结果 `DEPLOY_NO_TASK_PROCESS=1`。本次没有启动相机、完整任务、底盘运动或机械臂；新的横移后实走 2 米和故障恢复仍需下一次真机日志确认。

## 记录 29：红条丢失后左移搜索与故障等待有界退出

- 时间：2026-08-08 18:06（机器人 CST）。
- 最新实机日志 `mission_20260808_170534.log` 显示开场避障完成并回到原行进线，抓取路线和超声波接近均完成；失败发生在 `moving-pose lateral alignment`。机械臂相机连续 3 次重连后仍返回 `target_not_found`，任务进入 `PICK_RED_BAR` 的 `FAULT_HOLD`。安全状态已稳定，但没有 `/tmp/lite3_fault_resume`，旧代码因此无限等待。该现象不是线程死锁。
- 用户确认的新抓取行为：原地连续 5 帧未找到红条时先重连相机，最多 3 次；仍未找到后以低速向左搜索最多 `1.00 m`。红条中心一进入相机画面水平中央 `1/3`，立即停止底盘并进入最终距离确认和机械臂抓取，不继续窄 ROI 横移，也不再等待 3 个稳定帧。

### 29.1 红条左移搜索

- `pregrasp_red_align.target_search_enabled=true`。
- 搜索速度 `target_search_speed_mps=0.04 m/s`，每步 `0.50 s`，每步后静止 `0.10 s` 并重新检查画面。
- 搜索累计里程上限 `target_search_max_distance_m=1.00 m`；中央接管范围为归一化水平坐标 `[0.3333333333, 0.6666666667]`。
- 搜索里程与原有精确对准 `max_strafe_distance_m=0.50 m` 分开累计，避免搜索刚开始就被旧上限截断；总对准时间上限由 `45 s` 调整为 `90 s`。
- 搜索动作保持前向位置和偏航反馈补偿。每个搜索脉冲结束都会停车；中央 `1/3` 首次检测到红条时再次明确停车，结果为 `aligned_after_left_search`，随后主任务执行 `<30 cm` 最终距离确认和抓取。
- 左移满 `1.00 m` 仍未找到时返回 `target_not_found_after_left_search`，不会继续无界横移。

### 29.2 故障等待和重试上限

- 主任务 `FAULT_HOLD` 单次最多等待 `fault_hold.max_wait_s=30 s`，每个任务状态最多重试 `fault_hold.max_retries_per_state=2` 次；稳定计数封顶为 `5/5`，不再输出 `220/5`。
- 安全状态仍自动恢复；巡检、抓取和放置状态仍允许在 30 秒内使用 `touch /tmp/lite3_fault_resume` 人工确认。超时或状态重试次数用尽后进入 `ABORT_SAFE`，执行统一停车、相机释放、机械臂和运动接口关闭，并以退出码 `1` 结束进程。
- 开场避障的持续 `HOLD`、相机反复打不开和运动接口恢复共用 `startup_avoidance.fault_hold_max_s=30 s`。超过上限抛出异常并交给主任务有界恢复，不再在避障运行循环内永久重连。
- `Ctrl+C` 进入统一清理；部署时清除了可能遗留的 `/tmp/lite3_fault_resume`，避免下次任务误消费旧信号。

### 29.3 测试与部署

- 本机 Python 3.8 `compileall` 通过；使用带 OpenCV 的 Python 3.12 运行全量 `427/427` 通过，耗时 `36.981 s`。
- 新增回归覆盖：3 次重连后才开始左移、中央 `1/3` 外继续搜索、进入中央 `1/3` 立即停车抓取、独立搜索里程上限、抓取故障 30 秒超时退出、每状态初次执行加 2 次重试、稳定计数封顶、避障持续 HOLD 超时、相机持续打不开超时和短暂故障恢复。
- 部署包：`I:\tmp\lite3_bounded_red_search_20260808_180021.tar`，SHA256 为 `8222a18c6be5b8cc3014b474848f31ac2cc39dcee19a5349023f4a1c918f4d55`。
- 部署脚本：`I:\tmp\deploy_lite3_bounded_red_search_20260808_180021.sh`，SHA256 为 `9c4895fe4216f0cb6294e20851890268b42f1c53954b6294dcc61bc54b2a3330`。
- 原子部署替换 `10` 个文件，备份目录为 `/home/ysc/my_sw/.codex_backups/robot_competition_before_bounded_red_search_20260808_180021`，原文件归档 SHA256 为 `24cf3c5f0f516a577b14d0d38f525459e2b62b5f63020b1f26539d0d80382398`。
- 机器人 Python `3.8.10` 编译和配置断言通过；远端定向测试 `234/234`，耗时 `67.888 s`；远端全量 `427/427`，耗时 `108.675 s`。
- 部署后独立核验关键文件哈希与本地一致，`REMOTE_TASK_PROCESSES=0`、`RESUME_SIGNAL_CLEAR=1`、`BACKUP_AND_TEST_LOGS=1`。部署和验证期间没有启动完整任务、相机、底盘或机械臂；真实左移方向、搜索里程和抓取效果仍需下一次实机日志确认。

## 记录 30：抓取后 0.80 米转向与放置字母双向搜索

- 时间：2026-08-08（当前会话）。
- 抓取成功后的既有顺序保持为：后退到抓取箱前约 `0.80 m`，完成抓取区偏航修正和纸箱中心修正，再按路线旋转 `180°` 面向投放区。
- 旋转后新增独立的连续超声波接近阶段：以 `0.08 m/s` 前进，经过滤波的前向超声波达到 `<=0.80 m` 后明确停车；停车前不采集或识别放置字母，避免远处标签提前触发放置。
- 字母搜索以 `0.80 m` 停车位置为横向原点。未识别到目标字母时先向物理左侧搜索 `1.00 m`；仍未找到则反向越过原点，继续搜索到物理右侧 `1.00 m`。
- 最坏路径为原点到左端 `1.00 m`，再从左端到右端 `2.00 m`，因此累计横移硬预算配置为 `3.10 m`，正常搜索几何累计为 `3.00 m`。
- 目标字母在任意图像水平位置首次可靠出现时立即返回完成，底盘保持停止并直接进入机械臂放置，不再执行目标居中和连续三帧确认。
- 新增并严格校验 `bilateral_search_enabled=true`、`lateral_search_each_side_m=1.00`、`immediate_complete_on_target_detection=true`；保留旧单向锚点导航作为关闭新开关时的兼容路径。
- 本地 Python 3.8 `compileall` 通过；放置任务定向测试 `37/37`、放置导航状态机 `38/38`、任务烟雾测试 `121/121` 通过；使用带 OpenCV 的 Python 3.12 执行全量回归 `435/435` 通过，耗时 `38.453 s`。
- 本轮静态测试只验证状态机、里程预算和停止顺序，不能替代机器狗真实超声波、横移方向、现场字母图像和机械臂放置验证。

### 30.1 部署结果

- 9 文件部署包：`I:\tmp\lite3_placement_bilateral_search_20260808_183221.tar`，SHA256 为 `bb34fb380d880ceb7e419ee1215080d35d56ba8583e264f07fdb08ccd3352f43`；原子部署脚本为 `I:\tmp\deploy_lite3_placement_bilateral_search_20260808_183221.sh`，SHA256 为 `e7484d781dfdc03b75a1d1f802699fb3a07a177ce067ac2c7270efe64fbf1a5e`。
- 部署前机器人上没有完整任务、避障、抓取转运或测试进程；机器人端先完成部署包哈希校验和 `bash -n`，再执行原子替换。
- 远端备份目录：`/home/ysc/my_sw/.codex_backups/robot_competition_before_placement_bilateral_search_20260808_183221`；原文件归档 SHA256 为 `8dedb3d8c5057ca5e24156844b90882bca090eb6d7c810daf8d159dd74789564`。
- 机器人 Python `3.8.10` 编译和配置断言通过，输出 `REMOTE_CONFIG_OK 0.8 0.8 1.0 3.1`；远端定向回归 `196/196` 通过，耗时 `65.833 s`；远端全量回归 `435/435` 通过，耗时 `110.133 s`。
- 部署脚本最终返回 `DEPLOY_REPLACED=9`、`DEPLOY_NO_TASK_PROCESS=1` 和退出码 `0`，没有启动完整任务、相机、底盘或机械臂。
- 部署完成后的独立 SSH 复核未执行：本机 WLAN 已切换为 `10.67.151.232/17`，不再位于机器人 `192.168.1.x` 网段，`192.168.1.103:22` 和 ICMP 均超时。该网络切换发生在部署脚本完整成功返回之后，不影响已经完成的原子部署和远端测试结果；重新接入机器人网段后仍应再次只读核对 9 个文件哈希和任务进程数。

## 记录 31：抓取双向搜索与放置两阶段视觉确认

- 时间：2026-08-08（当前会话）。
- 抓取区在三次相机重连仍无红条后，以当前位置为原点先向左 `0.50 m`、再穿过原点到右侧 `0.50 m`，累计硬预算 `1.50 m`；只有满足运动资格和稳定条件的红色候选才能结束搜索。
- 红条进入中央 `1/3` 后仅停车并退出搜索模式，不再直接判定抓取对准成功；随后继续使用 `roi=[0.42,0.55,0.58,0.85]` 精对准并要求连续 `3` 帧严格稳定。
- 放置区正确字母阈值提高到 `0.60`。看到非目标字母时按 A/B/C/D 顺序、`0.50 m` 格距做单次最多 `0.50 m` 的粗横移；完全无字母时保留左右各 `1.00 m` 的有界搜索。
- 正确字母进入中央 `1/3` 并连续确认 `3` 帧后，新增独立 `FINAL_APPROACH` 动作前进 `0.30 m`；里程计确认到位后重新采图，目标进入 `45%–55%` 并再次连续确认 `3` 帧才进入机械臂放置。
- 第一次放置缓存目标字母的实测净横移；第二次根据缓存和固定格距预测目标位置做粗定位，最终视觉确认不能省略。
- 本轮代码先在本地完成，Python 3.8 语法编译和本地严格配置检查通过；使用带 OpenCV 的 Python 3.12 执行本地全量回归 `441/441` 通过，最终复核耗时 `39.645 s`。随后已按 31.1 部署到机器狗，部署过程未启动完整任务、相机、底盘或机械臂。

### 31.1 机器狗部署结果

- 部署目标：`ysc@192.168.1.103:/home/ysc/my_sw/robot_competition`；机器狗 Python 版本为 `3.8.10`。
- 10 文件部署包：`I:\tmp\lite3_search_optimization_20260808_203924.tar`，SHA256 为 `2dc45e4714cc4696afbf299fa9ca148f2ce6c56812c5891b4bcc6dd47c147c00`；部署脚本为 `I:\tmp\deploy_lite3_search_optimization_20260808_203924.sh`，SHA256 为 `97c6aa91bc9adee4415caf09bad26f8baf5928ab5d0102265c625ca4885c9b3a`。
- 覆盖前备份：`/home/ysc/my_sw/.codex_backups/robot_competition_before_search_optimization_20260808_203924`；原文件归档 SHA256 为 `28e0aa3314455e090743ea15f344836a3ae95c0cf82915b8b1375b522fe786f8`。
- 远端包校验、逐文件校验、Python 3.8 编译和严格配置断言均通过，配置输出为 `REMOTE_CONFIG_OK 0.5 1.5 0.6 0.3 0.5`。
- 远端定向测试 `293/293` 通过，耗时 `76.408 s`；远端全量回归 `441/441` 通过，耗时 `114.809 s`；部署结果为 `DEPLOY_REPLACED=10`、`DEPLOY_NO_TASK_PROCESS=1`。
- 部署后独立 SSH 复核确认 10 个远端文件 SHA256 与本地逐一一致，备份存在，`/tmp/lite3_fault_resume` 已清除，未发现完整任务、避障、抓取或测试进程。本次没有启动机器狗运动，搜索和放置效果仍需下一次真机任务日志确认。

## 记录 32：抓取红块与红色障碍物颜色解耦

- 时间：2026-08-08（当前会话）。
- 用户现场图和最近一次抓取相机帧均确认：画面左侧是红色障碍物，右侧才是待抓取红色方块。最近实拍中障碍物主体主要为 `HSV H=3–7, S=171–204`，方块主体主要为 `H=172–179, S=99–130`。
- 仅修改抓取专用 `strip_detector_grasp_config.json`：删除低色相 `H=0–10` 范围，并把高色相下限由 `156` 收窄到 `168`，最终只接受 `H=168–179, S=45–255, V=50–255`。开场避障视觉与超声波参数未修改。
- 真实方块第一次进入画面中央 `1/3` 时，线性尺寸约为参考值的 `0.5855`；原 `strict_motion_min_linear_size_ratio=0.70` 会漏掉正确方块。因此该门槛同步改为 `0.55`，连续跟踪、下半画面限制、中央 `1/3` 接管和最终 ROI 连续三帧确认保持不变。
- 回放最近一次保存的全部 `42` 帧：第 20 帧低色相障碍物有效红色候选为 `0`；第 42 帧两个真实方块均保留；结合稳定跟踪和运动资格后，首个合格且进入中央 `1/3` 的方块出现在第 41 帧，尺寸比为 `0.5855`。
- 新增合成 HSV 回归，固定验证低色相障碍物被排除、高色相方块被保留；新增实拍尺寸比 `0.5855` 的接管回归，并同步配置契约断言。
- 本机 Python 3.8 `compileall` 通过；抓取对准模块 `93/93`、最终全量 `443/443` 通过，全量耗时 `38.796 s`。
- 本轮只完成本地修改和历史帧回放，尚未部署到机器狗，也没有启动相机、底盘、机械臂或完整任务。真机效果需部署后用新一轮任务日志确认。

## 记录 33：先找真实红块再接近箱体，持续左移并补偿漂移

- 时间：2026-08-08（当前会话）。
- 本记录以用户最新现场判定为准：上次抓取相机保存的 `42` 帧中没有真实红色方块。记录 32 将第 41 帧高色相候选解释为方块、并把运动接管尺寸比降为 `0.55` 的结论作废；该版本未部署到机器狗。
- 上次实机并不是找到红块后停车。程序在三次相机重连后向左搜索，名义左移 `0.44 m` 时前后漂移达到 `0.152 m`，超过 `max_forward_drift_m=0.15 m`，以 `forward_drift_limit` 结束抓取前对准。根因是正式配置虽然启用姿态保持，却把 `max_vx_correction_mps` 设为 `0`，只能监测漂移而不能纠偏。
- 抓取前顺序改为：移动姿态获取真实红块 -> 红块进入中央 `1/3` 后停车 -> 前向超声波接近目标箱体到 `28.5 cm` -> 重新创建视觉跟踪器并执行最终窄 ROI 横移 -> 最终距离复核 -> 机械臂抓取。超声波不再在红块出现前把任意前方障碍物直接解释为抓取箱体。
- 初次无目标搜索保留连续 `5` 帧和最多 `3` 次相机重连；之后只向左搜索，不再在 `0.50 m` 处反向右扫，左移上限为 `1.00 m`。已确认无方块的前 `0.50 m` 作为初次获取死区，越过后才允许接管目标。
- 搜索阶段只允许当前帧仍达到参考尺寸 `70%`、位于下半画面且严格稳定的目标结束搜索。画面边缘候选不再提前锁轨，`strict_tracking_min_linear_size_ratio=0.50` 只在目标正式锁定后用于短时连续跟踪；宽松候选不能结束搜索。
- `max_vx_correction_mps` 改为 `0.04 m/s`。左移搜索脉冲即使前后漂移暂时超过 `0.15 m` 也不再直接退出，而是继续横移并用闭环速度拉回前后位置；最终精对准阶段仍保留 `0.15 m` 漂移保护。
- 接近 `28.5 cm` 的停止条件由单个超声波样本改为连续 `3` 个有效样本，任一中间样本回到阈值外都会清零重新确认；接近后还必须重新视觉精对准和执行 `<30 cm` 最终距离复核。
- 使用生产检测器回放上次全部 `42` 帧：结果为 `camera_read_failed`（保存帧已耗尽），`selected_track_id=None`，没有返回 `target_acquired`；执行 `22` 个左移脉冲、`0` 个右移脉冲，名义左移 `0.44 m`。这证明上次画面结束点不会再被误判为找到方块，后续新帧到来时仍会继续向左。
- 新增回归覆盖：`0.5855` 误目标不得结束左移、初次 `0.50 m` 无目标死区、搜索漂移超过 `0.15 m` 时输出前后纠偏而不是退出、搜索/接近/精对准顺序、连续三帧超声波确认及中间样本复位。
- 本机 Python 3.8 `compileall` 通过；抓取相关定向回归 `224/224`、最终全量 `446/446` 通过，全量耗时 `39.103 s`。
- 本轮代码和历史帧回放先在本地完成，随后已按记录 34 部署到机器狗；部署过程没有启动相机、底盘、机械臂或完整任务。真实横移方向、目标出现位置、纠偏效果和箱体 `28.5 cm` 距离仍需下一轮实机日志确认。

## 记录 34：真实红块优先和 28.5 cm 接近流程部署

- 部署时间：2026-08-08 22:08（机器狗 CST）；目标为 `ysc@192.168.1.103:/home/ysc/my_sw/robot_competition`，机器狗 Python 为 `3.8.10`。
- 第一次 10 文件部署包为 `I:\tmp\lite3_real_red_then_285_20260808_215746.tar`，SHA256 为 `a2238764a5d0df5bfde8d55ce5177d33870ee928ab5640a072a612928b9a5cbf`；脚本 SHA256 为 `56acdb699a9275c4614075b82d762714dd58689f10a81c5031d6f8193c2115a3`。覆盖前备份为 `/home/ysc/my_sw/.codex_backups/robot_competition_before_real_red_then_285_20260808_215746`，原文件归档 SHA256 为 `0476e28f743885d8830e0a0f0fba0876b292a5b6145afc705039730964cf41f7`。
- 第一次部署的定向回归 `224/224` 通过，但远端旧 `tests/test_arm_runtime_safety.py` 仍断言抓取红色下限为 `[0,60,50]`，与已收窄的 `[168,45,50]` 冲突，导致全量回归 `445/446`。脚本随后输出 `DEPLOY_ROLLBACK_RESTORED=10`，独立复核确认 10 个运行文件全部恢复为覆盖前哈希，机器狗没有停留在半部署状态。
- 第二次将同步后的 `tests/test_arm_runtime_safety.py` 纳入部署，共 11 个文件。部署包为 `I:\tmp\lite3_real_red_then_285_v2_20260808_220410.tar`，SHA256 为 `ae53faf294db1d6e7c0b131eb29dcfecfd57487219cc48cc9c8f81919b79e326`；脚本为 `I:\tmp\deploy_lite3_real_red_then_285_v2_20260808_220410.sh`，SHA256 为 `a907b761d5085cf5056d0ad5ca68aeff53eba40745b2c4d35aed7096c8a68db9`。
- 第二次覆盖前备份为 `/home/ysc/my_sw/.codex_backups/robot_competition_before_real_red_then_285_v2_20260808_220410`，原文件归档 SHA256 为 `f9ca42e6128690bc9c74f9fc038bc0b6f2286b13331cff1113b1227db59c726a`。包哈希、`bash -n`、逐文件哈希、Python 3.8 编译和严格配置断言均通过；配置输出确认严格尺寸比 `0.70`、单向左移 `1.00 m`、初次搜索死区 `0.50 m`、前后纠偏 `0.04 m/s`、接近距离 `0.285 m`、连续停止样本 `3` 和抓取红色范围 `H=168-179`。
- 远端定向回归 `241/241` 通过，耗时 `62.723 s`；远端全量回归 `446/446` 通过，耗时 `114.164 s`；最终输出 `DEPLOY_REPLACED=11`、`DEPLOY_NO_TASK_PROCESS=1` 和退出码 `0`。
- 部署后独立 SSH 复核确认 11 个文件 SHA256 与本地逐一一致，备份和测试日志存在，`ACTIVE=0`、`RESUME_SIGNAL_CLEAR=1`。本次未启动完整任务、相机、底盘或机械臂；软件部署成功不代表真实搜索方向、红块识别、箱体测距和抓取动作已经通过真机验证。

## 记录 35：避障改为 1.5 米，抓取先到 28 cm 再持续左移

- 最新完整任务日志为 `logs/full_mission/mission_20260808_221628.log`，对应避障日志为 `startup_avoidance_runs/20260808_221644_516a955e.jsonl`，抓取前横移证据为 `pregrasp_align_runs/20260808-222002-614341`。任务完成 1 次开场避障和 4 个巡检点后，在 `PICK_RED_BAR` 失败。
- 本轮所谓“原地踏步”不是线程死锁。抓取对准保存了 `70` 帧，全部只产生宽松候选，没有任何严格稳定轨迹；候选最大框边为 `48 px`，严格运动接管门槛约为 `94.2391*0.70=65.97 px`。因此真实红块从未被锁定，程序执行 `50` 个 `0.04 m/s`、`0.50 s` 的短左移脉冲，用速度乘时间记满名义 `1.00 m`，耗时 `78.825 s` 后返回 `target_not_found_after_left_search`。
- 旧搜索没有用横向里程证明机器狗实际走满 `1.00 m`，且 `0.04 m/s` 短脉冲容易只触发步态而没有明显平移。失败后主任务进入 `FAULT_HOLD`，持续发送零速度等待人工恢复；`30.2 s` 超时后以退出码 `1` 正常退出。因此现场看到的是低速短脉冲加 30 秒零速等待，不是进程永久卡死。
- `startup_avoidance.decision.finish_forward_m` 从 `2.00 m` 改为 `1.50 m`。无障碍时从避障入口前进 `1.50 m` 后结束；遇到障碍时从最后一次横移完成点重新计算，实际前进 `1.50 m` 后回到第一次绕障前的原始行进线；直行过程中遇到新障碍仍优先重新绕行并清零旧 PASS 累计。
- 抓取前顺序改为：可选纸箱偏航 -> 前向超声波连续 3 个有效样本确认到 `28 cm` -> 红块获取和持续向左搜索 -> 清空旧轨迹后窄 ROI 精对准 -> 最终距离复核 -> 机械臂抓取。旧的“先寻找红块、再靠近到 28.5 cm”顺序已取消。
- 靠近后红块搜索速度从 `0.04 m/s` 提高到正常横移速度 `0.08 m/s`，单步从 `0.50 s` 改为 `1.00 s`，步间额外等待从 `0.10 s` 改为 `0`。靠近后的旧 `0.50 m` 无目标死区改为 `0`，目标进入画面中央 `1/3` 即可接管。
- 新增 `target_search_until_found=true`。正式单向左移不再因名义 `1.00 m` 或对准总时长 `90 s` 返回目标未找到，也不再因此进入 `FAULT_HOLD`；仍保持严格尺寸 `70%`、下半画面、稳定轨迹和中央 `1/3` 条件，避免把小反光和红色障碍物当作抓取目标。该配置意味着目标始终不存在时会持续左移，必须由现场人员保证左侧空间足够并保留人工急停。
- 本机 Python 3.8 语法编译通过；带 OpenCV 的 Python 3.12 定向回归 `249/249` 通过、全量 `448/448` 通过，全量耗时 `40.087 s`。
- 12 文件部署包为 `I:\tmp\lite3_avoid150_approach28_continuous_left_20260808_223741.tar`，SHA256 为 `ca44ee6bc05c6eef2d86dbc02c2ff22bbe35e28c9cf4df8c72e27245c5ca9e39`；原子部署脚本为 `I:\tmp\deploy_lite3_avoid150_approach28_continuous_left_20260808_223741.sh`，SHA256 为 `9bdd5950721b943a4c6233fa2e8b96545b8c6f447fc30f7083cb4cc83eab8abe`。
- 机器狗覆盖前备份为 `/home/ysc/my_sw/.codex_backups/robot_competition_before_avoid150_approach28_continuous_left_20260808_223741`，原文件归档 SHA256 为 `0a4d53193dae752c9cbfe4e4b355ba15ff1f8770915943087e9be2ef77987184`。远端严格配置输出为 `REMOTE_CONFIG_OK 1.5 True 0.08 1.0 0.0 0.0 0.28 3`。
- 远端定向回归 `249/249` 通过，耗时 `71.272 s`；远端全量回归 `448/448` 通过，耗时 `115.588 s`；部署结果为 `DEPLOY_REPLACED=12`、`DEPLOY_NO_TASK_PROCESS=1` 和退出码 `0`。
- 部署后独立 SSH 复核确认 12 个文件 SHA256 与本地逐一一致，备份和测试日志存在，`ACTIVE=0`、`RESUME_SIGNAL_CLEAR=1`。本次没有启动完整任务、相机、底盘或机械臂；`1.50 m` 实际避障里程、`28 cm` 停止和持续左移效果仍需下一轮真机日志确认。
