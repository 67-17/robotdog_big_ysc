# Robot Competition 项目说明

## 1. 文档范围

本文档对应本地完整代码备份：

```text
C:\Users\18054\Desktop\competition\robot_competition_full_code_backup_20260806
```

备份来源提交为 `5537673`，生成日期为 2026 年 8 月 6 日。该目录包含项目代码、配置、脚本、测试和随仓资料，不包含 Git 历史、Python 虚拟环境、缓存和现场运行日志。

当前代码只完成了本地实现和测试，没有推送 GitHub，也没有部署到机器狗。文中涉及运动方向和视觉效果的结论，凡标明“待实机验证”的项目，都不能视为已经在机器狗上确认。

## 2. 项目用途

本项目用于完成 2026 四足大型组比赛任务。机器狗从起点出发后依次完成：

1. 穿越随机障碍区。
2. 到四个巡检点识别区域字母和仪表盘状态。
3. 汇总异常区域。
4. 前往抓取区，通过前向超声波和红条视觉完成底盘对准。
5. 控制机械臂抓取红条。
6. 前往放置区，识别 A、B、C、D 字母并移动到目标字母前。
7. 控制机械臂放置红条。
8. 返回抓取区，重复第二次抓取和放置。
9. 停车并收纳机械臂，等待人工接管。

项目不使用激光雷达导航。运动主要依靠预设路线、腿部里程计、IMU、前向超声波和相机完成。

## 3. 主要目录和文件

| 路径 | 作用 |
| --- | --- |
| `mission_lite3/mission.py` | 完整比赛任务状态机，串联巡检、抓取、运输和放置 |
| `mission_lite3/run_mission.py` | 主程序命令行入口 |
| `mission_lite3/config/field.yaml` | 场地坐标、参考点和实际执行路线 |
| `mission_lite3/config/robot.yaml` | 网络、ROS2、相机、运动、安全、视觉和机械臂参数 |
| `mission_lite3/config_loader.py` | 加载配置并在运动前检查配置合法性 |
| `mission_lite3/lite3_motion.py` | ROS2 和 UDP 运动控制统一接口 |
| `mission_lite3/state_reader.py` | 读取里程计、IMU、电量和前后超声波状态 |
| `mission_lite3/camera.py`、`wide_camera.py` | 前向相机和广角相机读取 |
| `mission_lite3/vision/` | 巡检字母、仪表盘和颜色识别 |
| `mission_lite3/inspection_runtime/` | 巡检识别、时序投票和结果绑定 |
| `mission_lite3/round_result.py` | 管理一轮巡检的异常区域结果 |
| `mission_lite3/box_approach.py` | 前向超声波靠近纸箱的距离策略 |
| `mission_lite3/pregrasp_red_align.py` | 抓取前红条视觉横移对准 |
| `mission_lite3/box_center_alignment.py` | 抓取区或放置区箱体中心识别与横移 |
| `mission_lite3/placement_letter_navigation.py` | A-D 字母视觉前移、搜索和横移决策 |
| `mission_lite3/pickup_transfer.py` | 两轮抓取、后退、运输和通道恢复相关逻辑 |
| `mission_lite3/arm/lite_arm.py` | 主任务与机械臂 runtime 之间的适配层 |
| `mission_lite3/arm/run_arm_task.py` | 项目内机械臂任务入口 |
| `mission_lite3/arm/runtime/` | 机械臂串口控制、红条识别、抓取、放置和示教工具 |
| `scripts/run_full_mission.sh` | 完整实机任务的推荐启动脚本 |
| `scripts/run_pickup_transfer_mission.sh` | 只测试两轮抓取和放置的入口 |
| `tests/` | 配置、状态机、视觉导航、机械臂安全和运动逻辑测试 |
| `26比赛资料/` | 比赛地图、题目和历史参考工程，不是正式运行入口 |
| `lite3资料/` | Lite3 手册及设备资料 |

正式任务代码集中在 `mission_lite3/`。不要从 `26比赛资料/DeepRobotDog-main/` 启动当前比赛流程，该目录只用于参考。

## 4. 运行环境与硬件接口

推荐部署目录：

```text
/home/ysc/my_sw/robot_competition
```

主要运行条件：

- Python 3.8 或更高版本。
- ROS2 Foxy 和 Lite3 的 `transfer_ros2` 服务。
- OpenCV、NumPy、Pillow、PySerial 和 PyYAML。
- 运动主机默认地址 `192.168.1.120:43893`。
- 感知主机默认地址 `192.168.1.103`。
- 前向相机默认地址 `rtsp://192.168.1.120:8554/test`。
- 机械臂默认串口 `/dev/ttyUSB0`。

ROS2 话题：

| 用途 | 话题 |
| --- | --- |
| 速度控制 | `/cmd_vel` |
| 腿部里程计 | `/leg_odom2` |
| IMU | `/imu/data` |
| 前向超声波 | `/us_publisher/front_distance` |
| 后向超声波 | `/us_publisher/rear_distance` |

安装 Python 依赖：

```bash
python3 -m pip install -r mission_lite3/requirements.txt
```

ROS2 包应使用机器狗系统环境，不建议通过 `pip` 安装。

## 5. 完整状态机流程

主状态机定义在 `mission_lite3/mission.py`，运行顺序如下。

### 5.1 启动检查

`BOOT_CHECK` 会初始化本轮巡检结果，等待运动状态、IMU 和必要的超声波数据，打开前向相机并读取首帧。任何必要链路不可用时，任务在发送路线运动前终止。

### 5.2 进入任务姿态

`STAND_AND_ARM` 当前默认认为机器狗已经站立，不重复发送起立或趴下切换命令。程序切换自主模式，并要求机械臂进入 `moving_pose.json` 定义的移动姿态。

### 5.3 穿越障碍区

正常配置下，`PASS_OBSTACLE` 不再执行 `field.yaml` 中旧的固定 8 步路线，
而是运行 `mission_lite3/startup_avoidance/`：

1. `/dev/video0` 持续只保留最新画面，HSV 检测红色锥桶。
2. 前向超声距离和锥桶画面区域共同决定是否进入绕行；目标连续确认 5 帧时机器狗停止平移。
3. 当前比赛策略固定向左绕行，锥桶进入右侧安全区并稳定 5 帧后低速前进通过。
4. 锥桶跟踪确认清除后，按 `/leg_odom2` 投影误差返回绕行前的行进线，误差不超过 `0.02 m` 并稳定 5 帧。
5. 每次回线后检查前向投影里程；未到 `2.40 m` 就继续巡航并寻找下一个障碍，遇到几个就执行几次避障。
6. 没有障碍时直接巡航；前向投影里程达到 `2.40 m` 时完成 0 次避障也可以正常结束。
7. 到达出口时如果正在绕行或回线，必须先完成当前障碍的通过和回线，不能中途交接。
8. 状态机随后进入 `INSPECT_LEFT_OBJECT`，执行原有 `inspect_stop_1_arrive` 到第一个巡检点。

默认巡航、绕行和通过速度分别为 `0.08 m/s`、`0.08 m/s` 和 `0.06 m/s`。
正前方/侧方触发距离为 `0.35 m` / `0.50 m`，`0.20 m` 为紧急停车下限。
图像、超声、里程计过期，跟踪歧义，未知近障，回线超时，相机失败或总运行超过
`120 s` 都会中止任务，且不会回退到盲目固定横移。逐帧证据保存到
`startup_avoidance_runs/*.jsonl`。

`field.yaml` 的旧 `pass_obstacle` 只在显式关闭 `startup_avoidance.enabled`
或使用忽略障碍调试参数时保留，不代表正式启用避障时的实际行进距离。

### 5.4 四点巡检

四个巡检点依次执行：

1. 到巡检点 1，识别字母和仪表盘。
2. 到巡检点 2，再次识别。
3. 到巡检点 3，再次识别。
4. 到巡检点 4，再次识别。

停车点编号不固定对应 A、B、C、D。字母以相机识别结果为准。每个稳定结果会写入当前轮次记录，主任务只接受本轮生成且 `ready=true`、`unknown_areas=[]` 的 `round_result.json`。巡检结果不完整时，抓取流程被阻止。

### 5.5 第一次抓取

`PICK_RED_BAR` 从异常区域列表取得第一个目标字母，然后执行：

1. 保持机械臂移动姿态进入抓取区。
2. 使用前向超声波靠近纸箱，目标约为 `0.285 m`。
3. 识别红条并根据水平误差控制机器狗横移。
4. 对准后再次检查前向距离，要求严格小于 `0.30 m`。
5. 调用机械臂 runtime 执行视觉对准、夹爪闭合和货运姿态。
6. 只有机械臂返回成功且确认持有物体，流程才允许继续。

抓取前红条横移速度为 `0.08 m/s`，单次脉冲按像素误差在 `0.15-1.00 s` 范围内调整。累计名义横移上限为 `0.50 m`，前后漂移上限为 `0.15 m`。目标连续丢失后会重连相机，达到重试上限则停车并中止抓取。

### 5.6 抓取后退出

夹住红条后，机器狗后退到前向超声波约 `0.80 m`。当前配置允许在超声波连续卡在 `0.28 m` 时使用腿部里程计完成受限后退，但最大里程计后退距离仍受 `0.55 m` 硬限制。

后退完成后，程序使用抓取区纸箱进行偏航矫正，并以画面宽度 `+/-3%` 的中心误差完成离开抓取区前的横移对准。

### 5.7 第一次字母放置导航

机器狗转向放置区后执行字母视觉导航。四个箱子按物理位置从左到右排列为：

```text
A  B  C  D
```

程序不再用固定的 A-D 横移距离完成正式投放，而是持续识别画面中的任意字母作为位置锚点。例如目标是 D，当前看到 B，则机器狗向物理右侧移动；目标是 B，当前看到 D，则向物理左侧移动。

当前字母导航规则：

- 识别到目标字母时，按其相对画面中心的位置横移。
- 目标中心误差不超过画面宽度 `5%`，并连续通过 3 个不同新帧后，才认为居中。
- 没有识别到任何字母且前向距离大于 `0.35 m` 时，以 `0.08 m/s` 向前搜索。
- 每轮前向搜索预算为 `0.10 m`，总前移预算为 `1.38 m`。
- 到达 `0.35 m` 仍没有字母时，停止前移并向物理左侧搜索。
- 横移搜索步长为 `0.10 m`，累计横移上限为 `1.05 m`。
- 单次目标居中修正不超过 `0.10 m`。
- 相机单次读取超时为 `0.50 s`，整个字母导航最长 `90 s`。
- 字母导航失败时停车并中止，机械臂不得继续放置。

每个被接受的相机帧都会先触发停车，再进行去畸变、字母识别、传感器读取和证据写入。准备恢复向前运动前，程序再次检查总时限、里程计、IMU 和前向超声波；若最新前向距离不大于 `0.35 m`，不会发送新的非零前进指令。

第一次字母导航实际测得的净横移距离会被记录。后续返回抓取区时使用该实测距离恢复通道，不使用固定的字母偏移量。

### 5.8 放置机械臂动作

到达目标字母中心后，机械臂使用 `place_reference.json` 中对应 A、B、C 或 D 的放置姿态释放红条。释放后先回收部分肩关节，再让肩关节和肘关节共同回到移动姿态，最后整理底座和腕关节。

### 5.9 第二次抓取和放置

`SECOND_PICK_PLACE` 使用第一次记录的实测净横移距离返回抓取通道，再以画面宽度 `+/-6%` 的误差对准抓取区纸箱。之后重新执行完整抓取、后退、运输、字母识别和放置流程。

第二次投放必须重新运行字母视觉导航，不能沿用第一次的目标位置判断。只有第一次实测的通道净横移用于返回抓取区。

### 5.10 结束

`FINISH_OR_SAFE_STOP` 停止底盘，确认机械臂未持有物体，然后收纳机械臂并等待人工接管。异常路径进入安全停止，不应继续抓取或放置。

## 6. 当前实际路线距离

`field.yaml` 中的 `scripted_route` 才是主流程实际使用的路线。`waypoints` 只用于理解和后续标定。

| 路段 | 动作 |
| --- | --- |
| 起点穿越障碍区 | 前向投影达到 `2.40 m`；区内遇到几个就逐个绕行几个，每次用 `/leg_odom2` 回原线 |
| 到巡检点 1 | 横移 `-0.65 m`，前移 `0.40 m` |
| 巡检点 1 到点 2 | 横移 `-1.18 m`，前移 `0.88 m`，转向 `+1.5708 rad` |
| 巡检点 2 到点 3 | 转向 `-3.1416 rad`，无平移 |
| 巡检点 3 到点 4 | 转向 `+1.5708 rad`，后退 `0.88 m`，横移 `-0.95 m` |
| 巡检点 4 到抓取区 | 转向 `+1.5708 rad`，前移 `1.35 m`，横移 `+1.45 m` |
| 抓取区到放置区 | 转向 `-3.1416 rad`，执行放置排姿态动作，再运行字母视觉导航 |
| 放置区返回抓取区 | 转向 `+3.1416 rad`，前移 `1.38 m`，再恢复第一次实测横移 |

正负方向随机器狗当前朝向变化，不能只看数值判断地图方向。路线注释中约定：初始朝地图左方时，前进为地图左方，横移正值为地图下方；抓取和放置阶段转向后，应以当时机身坐标解释。

参考点坐标如下，但主任务不直接按这些坐标导航：

| 点位 | x | y | yaw |
| --- | ---: | ---: | ---: |
| 起点 | `0.00` | `0.40` | `3.1416` |
| 障碍入口 | `-1.10` | `0.80` | `3.1416` |
| 障碍出口 | `-1.80` | `1.45` | `3.1416` |
| 下方巡检区 | `-2.35` | `1.90` | `3.1416` |
| 上方巡检区 | `-2.35` | `3.55` | `3.1416` |
| 抓取区 | `-0.75` | `2.65` | `1.5708` |
| 放置区 | `-0.65` | `4.90` | `1.5708` |

## 7. 机械臂代码和参数

机械臂正式代码位于：

```text
mission_lite3/arm/runtime/
```

主要文件：

| 文件 | 作用 |
| --- | --- |
| `arm_task.py` | 抓取、放置、预检、回零和诊断任务状态机 |
| `arm_grasp.py` | 抓取动作和视觉伺服 |
| `place_controller.py` | A-D 放置动作 |
| `strip_detector.py` | 红条检测 |
| `test.py` | 串口、关节、夹爪和移动姿态底层工具 |
| `moving_pose.json` | 机器狗移动时机械臂姿态 |
| `grasp_reference_square_face.json` | 抓取参考姿态和视觉参考 |
| `place_reference.json` | A-D 各自的放置关节目标 |
| `strip_detector_grasp_config.json` | 抓取阶段红条检测参数 |
| `camera_calibration.json` | 当前项目机械臂相机标定参数 |
| `ost.yaml` | runtime 中保留的另一份标定文件，但当前 `robot.yaml` 的 `arm.calibration` 没有指向它 |

当前项目实际配置使用：

```text
mission_lite3/arm/runtime/camera_calibration.json
```

因此，不能仅因为目录中存在 `ost.yaml` 就认定运行时使用它。若要切换标定文件，应同时检查 `robot.yaml`、runtime 参数传递和实际启动日志。

让机械臂进入移动姿态：

```bash
cd /home/ysc/my_sw/robot_competition
python3 -m mission_lite3.arm.run_arm_task moving-pose --port /dev/ttyUSB0
```

机械臂预检：

```bash
scripts/check_arm_devices.sh
scripts/run_arm_task.sh preflight
```

单独执行抓取和放置：

```bash
scripts/run_arm_task.sh grasp
scripts/run_arm_task.sh place --slot A --object-held
```

`A` 可以替换为 `B`、`C` 或 `D`。机械臂曾有肘关节不到位以及 `move=1` 状态粘住的现场记录，当前备份不能代替实机复核。正式运行前必须重新验证供电、串口、移动姿态、抓取和四个放置姿态。

## 8. 启动方法

### 8.1 开发电脑干跑

干跑不会发送真实硬件指令：

```bash
python3 -m mission_lite3.run_mission --dry-run --skip-arm
```

### 8.2 完整实机任务

机器狗回到比赛起点、夹爪清空、周围人员撤离且急停人员就位后运行：

```bash
scripts/run_full_mission.sh
```

只测试底盘、巡检和播报，不驱动机械臂：

```bash
scripts/run_full_mission.sh --skip-arm
```

ROS2 速度链路不可用时才考虑：

```bash
scripts/run_full_mission.sh --udp-fallback
```

### 8.3 两轮抓取和放置专项测试

操作人员应先把机器狗放在抓取区约 `0.80 m` 位置。先干跑：

```bash
scripts/run_pickup_transfer_mission.sh --targets A D
```

确认现场安全后才允许实机运行：

```bash
scripts/run_pickup_transfer_mission.sh --robot --yes --targets A D
```

两个实机入口都使用 `/tmp/lite3_motion_test.lock`。锁在 5 秒内无法取得时，脚本打印错误并以状态码 `3` 退出，不会启动 Python 主任务。不要绕过脚本直接用 `python3 -m mission_lite3.run_mission --robot` 启动正式任务，否则会失去统一运动互斥保护。

## 9. 测试和日志

完整本地检查入口：

```bash
bash check_robot_runtime.sh
```

该脚本依次执行 Python 编译、全部 `unittest` 和 shell 语法检查。

当前代码的本机验证结果：

- 字母导航及相关专项测试 `120/120` 通过。
- 启动避障专项测试 `13/13` 通过，覆盖 0 个、1 个和多个障碍物。
- 完整测试共 376 项，其中 375 项通过。
- 唯一失败来自 Windows 环境缺少 Unix `true` 命令，与运动、抓取和字母导航无关。
- 本机验证没有连接相机、ROS、串口或机器人，不等同于真实场地通过。
- 两个实机启动脚本均通过 `bash -n` 语法检查。

常见运行结果目录：

| 路径 | 内容 |
| --- | --- |
| `evidence/` | 巡检证据图片 |
| `grasp_runs/` | 机械臂抓取过程日志 |
| `logs/` | 最近一次机械臂任务结果 |
| `pregrasp_align_runs/` | 抓取前红条横移日志 |
| `placement_letter_navigation_runs/` | 放置区原图、去畸变图、标注图和逐帧事件 |
| `pickup_transfer_runs/` | 两轮抓取放置专项测试结果 |
| `startup_avoidance_runs/` | 起步避障逐帧决策、传感器年龄、跟踪和回线误差 |

这些目录通常在运行时生成，不属于代码备份内容。

## 10. 上机前必须确认

以下项目尚需在真实机器狗上完成，不能由本地测试替代：

1. 确认 `physical_left_strafe_sign=1` 与机器狗实际左右横移方向一致。
2. 使用真实 A、B、C、D 标牌检查前向相机、去畸变和字母置信度。
3. 联合检查前向超声波、里程计和 IMU 的更新频率与方向。
4. 测试当前识别字母和目标字母的全部 16 种组合。
5. 在机器狗 Linux 环境验证 `flock` 锁竞争和超时退出。
6. 复核机械臂串口、相机设备、`moving_pose.json`、抓取参考和 A-D 放置姿态。
7. 验证肘关节出现 `move=1` 时的任务中断和人工处理方式。

这些检查通过前，应使用 `--skip-arm` 验证底盘流程，并保持机械臂禁用。任何真实运动测试都必须安排人员持有物理急停，机器狗与纸箱之间不得站人，也不得放置易损物品。

## 11. 当前代码状态

- 本地备份来源提交：`5537673`。
- 备份生成时原提交包含 259 个受版本管理文件。
- 本文档是备份完成后新增的说明，不属于原提交内容。
- 当前代码没有推送到 GitHub。
- 当前代码没有部署到机器狗。
